# 自研代码清单（2026-08-16 盘点 · 08-16 晚更新）

> 目的：回答"系统里有多少功能/环节是自研代码实现的"，并为"尽量不用自研代码"提供可执行的去自研化路线图。
> 盘点方式：D:/scripts、项目 scripts/、D:/docker/open-webui、计划任务、docker 服务实测确认。
> ⚠️ 更新（08-17）：Filter **v1.10.2（199 行）**——身份注入已剥离（原生 user.variables 接管），其余 5 项硬兜底保留（剥离探索见手册 v2 §10.4）；Dify 已退场/kb_dream 停用/oikb 接管；耦合度评估见《智能中枢_OWUI迁移升级审计手册_v2》§6。

---

## 一、总览

| 类别 | 数量 | 说明 |
|---|---|---|
| 运行中 · 业务功能依赖 | 2 | 姜维 Filter（核心）+ family-bridge |
| 运行中 · 运维基础设施 | 6 | ModelGuard、ollama_serve_env、custom.js、server_selfcheck、owui_weekly_review、**oikb（官方工具，非自研）** |
| 闲置 / 待归档 | 3 | rerank_server、ollama_debug_proxy、诊断小工具（**已移入 D:\scripts\_archive_20260816\**） |
| 一次性工具（已完使命） | 2 | dify_export_to_owui、poc_compare |
| 已停用（历史管道） | 3 | dify_kb_bridge.py、**kb_dream（08-16 停用）**、**Dify 全家桶（08-16 down，卷保留）** |

**一句话**：当前"活着的"自研代码 = **2 个业务依赖 + 6 个运维基础设施**；其中姜维 Filter 是唯一"不可挂"的核心。**Dify 已退场（08-16），知识库全归 OWUI。**

---

## 二、运行中 · 业务功能依赖（去自研重点）

### 1. 姜维 Filter `filter_jiangwei_kb.py`
- **位置**：`D:\Users\Danny\Documents\家庭智能中枢\scripts\filter_jiangwei_kb.py`（**v1.10.2，199 行**，工作区=容器一致；v1.8.0 起身份注入已删，由原生 user.variables 接管）
- **部署**：OWUI Function（type=filter，id=`jiangwei_kb_attach`，is_active=1 + is_global=1）
- **职责**：① 身份感知（注入当前登录用户，王俊威=主公）② 自动附加 7 知识库 + 兼容字符串 files（修复 OWUI 0.10.2 前后端 bug）③ **确定性联网**（实时关键词→Filter 内调 SearXNG→排序注入）④ **确定性写记忆**（记住XXX→Filter 直写 memory 表，绕过模型工具不稳定）⑤ 实时关键词中英文表
- **运行方式**：OWUI 每次 `model==jiangwei` 请求必经 inlet（内嵌于 OWUI 进程）
- **依赖方**：姜维全部能力（身份/知识/记忆/联网）——**去它姜维立刻退化成裸 9B**
- **去自研替代路径**：升级 OWUI 版本，验证①模型绑定知识库是否自动附加 ②是否支持 per-model web_search 开关 ③是否支持 per-user 提示词注入；三者都满足才可去掉 Filter（另需原生 Memory/WebSearch 工具在本地模型的可靠性达标）
- **优先级**：🟡 观察——升级 OWUI 时一并验证，不主动动；⚠️ 红线：勿继续膨胀成第二套管道（详见手册 §6/§7）

### 2. family-bridge 网关 `bridge_server.py` + `core.py`（已退役 08-16）
- **位置**：`D:\docker\family-bridge\`（原生 Python 服务 :8000，**进程已停、自启已停、README_已退役 标注**）
- **历史职责**：OpenAI 兼容网关——把「模型 + 联网 + RAG」封装成标准接口供外部客户端
- **退役依据**（08-16 实测）：① **24h 零调用**（最后请求 08-15 22:54）② Dify 链路已死（Dify 同天退场）③ WorkBuddy 已直连 Ollama/云端（models.json 死条目已清，备份 .bak-20260816-bridge-retire）④ n8n 无 :8000 引用
- **可回滚**：目录保留原地；恢复需 Dify 已不可用，无实际意义
- ⚠️ 遗留：防火墙规则 family-bridge-8000 删除失败（需管理员）

### 3. 记忆固化 `dream/kb_dream.py`（已停用 08-16）
- **位置**：`D:\scripts\dream\kb_dream.py`（计划任务 `AIHomeServer-Dream`，每日 3:00，**08-16 已 Disable**）
- **职责**：把对话新事实 → 草稿（置信度分级）→ 固化到知识库 + 冲突检测
- **历史依赖方**：家庭共享记忆的每日固化（Dify「家庭档案」库上传）
- **停用原因**：Dify 退场（08-16 主公拍板）→ 上传环节失效；**上传已由 oikb 接管**（`AIHomeServer-OIKBSync` 每日 3:00 增量同步 memory_docs → OWUI 家庭档案库）
- **去自研替代路径**：整理环节（对话新事实→memory_docs）待 Phase 2「KB Sync + OWUI Memory」路线定夺；必要时恢复 Dream 任务只保留整理逻辑

---

## 三、运行中 · 运维基础设施（保留，不算业务依赖）

| # | 代码 | 位置 | 职责 | 替代判断 |
|---|---|---|---|---|
| 4 | ModelGuard v2.1 | `D:\scripts\openwebui_model_guard.py`（ps1 调度，每日 3:05） | 守护模型体系：默认姜维 / 保 qwen3.5:9b / 清白名单 / 保 jiangwei 知识库权限 / **固化 function_calling=native + builtinTools** / 账号清单 | 保留——OWUI 升级修复 model 解析 bug 后或可简化，但属低风险运维守护 |
| 5 | Ollama 启动 | `D:\scripts\ollama_serve_env.cmd`（计划任务启动 serve） | 显式注入 `OLLAMA_MODELS/HOST/MAX_LOADED_MODELS`（app 不继承 env 的坑） | 保留——Machine 级 env 无权限；Docker 化成本高；属运维底座 |
| 6 | 回复指标显示 | `D:\docker\open-webui\custom.js`（docker-compose 挂载 /app/build/custom.js:ro） | 前端注入「模型名\|耗时\|tok/s」 | 保留——纯前端增强，不参与逻辑；OWUI 无原生等价 |
| 7 | 健康巡检 | `D:\scripts\server_selfcheck.ps1`（每周） | 只读服务巡检 | 保留或并入 home-hub-health-check skill |
| 8 | **周评估（新增 08-16）** | `D:\scripts\owui_weekly_review.py` + `.ps1`（每周日 22:00） | 近 7 天聊天/记忆/联网/性能/异常 → `D:\scripts\reports\owui-weekly-*.md` | 保留——运维守护，只读库，耦合低 |
| 9 | **oikb（官方工具，非自研）** | `D:\scripts\oikb\`（venv + .oikb.yaml + oikb_sync.ps1，每日 3:00） | 增量同步 memory_docs → OWUI 家庭档案库（SHA-256 diff）| 官方配套——替代 kb_dream 上传环节；详见《OWUI能力深挖与最佳实践》§3 |

---

## 四、闲置 / 待归档（可直接清）

| # | 代码 | 位置 | 确认结论 |
|---|---|---|---|
| 8 | rerank 桩 | `D:\scripts\rerank_server\`（rerank_server.py + start_rerank_server.bat） | **无进程监听**；OWUI `rag.reranking_engine=''` 不引用；Dify env 无 rerank 变量 → 彻底闲置 |
| 9 | Ollama 调试代理 | `D:\scripts\ollama_debug_proxy.py` | 诊断用，无运行依赖 |
| 10 | OWUI 诊断小工具 | `D:\scripts\owui_check_default_model.py` / `owui_check_login_config.py` | 一次性诊断，无运行依赖 |

> **执行**：本次已移入 `D:\scripts\_archive_20260816\`（见 §五 执行记录）。

---

## 五、一次性工具（已完使命，保留在项目 scripts/ 备查）

| 代码 | 使命 | 状态 |
|---|---|---|
| `scripts/dify_export_to_owui.py` | Dify 113 文档导出 + 上传 OWUI | 已完成（08-15） |
| `scripts/poc_compare.py` | POC 双盲比对评测 | 已完成（08-15） |
| `D:\docker\open-webui\dify_kb_bridge.py` | 旧姜维管道（已停用 is_active=0） | 停用，Phase 5 后随 Dify 归档 |

---

## 六、去自研化路线图（按可执行性排序 · 08-17 更新）

| 序 | 动作 | 前置条件 | 风险 | 状态 |
|---|---|---|---|---|
| 1 | **归档闲置代码**（debug proxy / 诊断工具） | 已确认无引用 | 极低 | ✅ 08-16 执行 |
| 2 | **kb_dream 退役** | 上传环节已由 **oikb** 接管 ✅（08-16）；整理环节待 Phase 2 KB Sync + OWUI Memory 闭环 | 中 | 🟡 上传已替代；整理待 Phase 2 |
| 3 | **family-bridge 退役** | ✅ **已退役（08-17）**：24h 零调用实测 + WorkBuddy models.json 清死条目 + 8000 关闭 + 自启停用 | 低 | ✅ 完成 |
| 4 | **姜维 Filter 身份注入剥离** | ✅ **已完成（08-17）**：`user.variables` 原生渲染接管，3 用户变量已设（王俊威=主公/Amanda/Shawn=少主），Filter v1.8.0 删身份段；**其余 5 项（联网/记忆/KB 附加/files）实测原生不可替代，Filter v1.10.2 保留兜底**（退役钥匙=更强本地模型或 OWUI query 生成可配强模型）| 中 | ✅ 完成（部分）|
| 5 | **姜维 Filter ①②（KB 附加/强制联网）剥离** | 切 legacy 后原生回归（v0.11 保留 `metadata.params.function_calling=='legacy'` 判定）；需实测原生搜索质量（loader 历史问题）| 高 | 观察（legacy 评估）|
| 6 | **Filter ④记忆写入 → background review** | compose 加 `ENABLE_MEMORY_BACKGROUND_REVIEW=true` + 多轮实测提取质量（0.6B task 模型）| 中 | 🟡 待实测 |
| 7 | **rerank 服务官方化** | 🔴 当前 9997 是**自研桩 rerank_server.py**（08-13 起跑，接口兼容 xinference）；08-16 尝试 xinference-local 官方失败（Windows worker 起不来）→ **待办：xinference docker 化**（官方镜像绕开 Windows 问题）| 中 | 🔴 待办（P-D 已启用但实现为自研桩）|
| 8 | **ModelGuard 简化/保留** | v0.11 实测「config fine」（08-17）→ 保留，schema 兼容 | 低 | 保留 |
| 9 | **周评估→OWUI Evaluation 评估** | OWUI 原生 Evaluation 页（深挖 §10）能力核对 | 低 | 观察 |
| 10 | **custom.js v2.2 回挂** | v0.11 UI 全面重设计，v2.1 依赖的 DOM 已变（升级时已卸载）；需先看新结构重写指标显示 + pwa 重生成 | 低（纯前端）| 🔴 待办（待主公确认）|

**原则**：去自研化 ≠ 一夜清零。**先清闲置（零风险）→ 官方替代（oikb✅/xinference 待办）→ 退业务依赖（bridge 可提前）→ Filter 留到 OWUI 升级时验证**。运维守护（ModelGuard/ollama 启动/周评估）属于"运维代码"而非"功能依赖"，保留合理。

---

## 七、执行记录

- 2026-08-16 上午：`D:\scripts\rerank_server\`、`ollama_debug_proxy.py`、`owui_check_default_model.py`、`owui_check_login_config.py` 移入 `D:\scripts\_archive_20260816\`。
- 2026-08-16 晚（Dify 退场收尾）：`dify_stream_diag.py`、`dify_rerank_restore.py`、`openwebui_chat_test.py`、`openwebui_v4_test.py`、`openwebui_stream_test.py` 移入 `_archive_20260816\dify_残留\`；`dify_console\` 已加 README 停用标注。
- 2026-08-16 晚（rerank 启用）：自研桩 `rerank_server.py` 从归档恢复至 `D:\scripts\rerank_server\`（xinference 官方 Windows 起不来，暂时回退；xinference docker 化列入路线图 #5）；注册表 Run 键 `AIHomeServerRerank` 登录自启。
