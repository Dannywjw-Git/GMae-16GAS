# GPU Maestro-显存指挥家 · 调度中心架构与交互设计 v2.9

> **产品总名**：GPU Maestro-显存指挥家（简称 GMae）— 显存编排专家，管"AI 生成工具怎么在消费级显卡上活得好"的那一层
> **核心引擎**：Prism Engine-棱镜引擎（简称 P-Eng）— 整个系统的核心价值所在
> **落地项目编号体系**：GMae-XXGAS（如 **GMae-16GAS** = GPU Maestro-16G-AI-Studio，16GB 单卡落地项目，大赛投稿项目；未来 GMae-24GAS / GMae-48GAS…）
> **调度中心**：vram-console（P-Eng 的具体实现，Web 服务 + Python 后端）
> **标语**：一卡，全模态（1 GPU，∞ AI）｜卖点：显存永不溢出
> **参赛赛道**：2026 上海开源软件应用创新大赛 — **智算云赛道**（官方三赛道之一，覆盖异构算力调度 / GPU 池化 / 推理加速，关注资源效率与部署可观测性；本项目属"算力协调"类，命中该赛道画像）
> 本文档是设计框架，定义"做什么"和"为什么"。具体技术实现（API、代码、配置）见执行文档。
> 最高指导：《显存管理最高指南》v1.3
> **版本**：v2.9（2026-08-27 交互设计重构：三层界面"少即是多"——日常仪表盘只读 / 动作大按钮 / 设置折叠层；自由度=可否决+可接管+可回看，操作≠配置。前序 v2.8：标语定稿"一卡，全模态/1 GPU，∞ AI/显存永不溢出"；P-Eng=vram-console 结构修正）
> 创建：2026-08-26 | 更新：2026-08-27

---

## 〇、文档定位

### 目标
为消费级 AI 服务器（首个参考实现：16GB 单卡 16GAS）设计统一的显存编排层，实现多模态 AI 服务（对话/文生图/图生图/文生视频/图生视频/文生音乐）的**可靠、高效、友好、主动编排**管理。

### 读者
- 系统维护者（理解整体架构，快速定位问题）
- 后续开发 Agent（按本设计实施，不偏离设计）
- 未来扩展者（加模型/加硬件/加功能时遵循框架）

### 范围
- 调度中心本身的架构设计（感知/账本/执行三层）
- 产品机制设计（门卫：登记簿与驱逐；指挥家：QoS/回收/队列）
- 人机交互界面设计（"显存指挥家"隐喻贯穿）
- 与 OWUI、ComfyUI、Ollama、Fooocus 的交互边界
- 不包含：具体模型工作流设计、OWUI 前端定制、Docker 部署细节

---

## 一、项目定位与大赛价值

### 项目关系与命名体系（2026-08-27 定稿）
```
GPU Maestro-显存指挥家（GMae）— 产品总名，消费级 AI 服务器显存编排专家
└── GMae-16GAS（GPU Maestro-16G-AI-Studio，简称 16GAS）
    — 首个落地项目编号（16GB 单卡，大赛投稿项目）
    └── vram-console — 调度中心，即 Prism Engine（P-Eng）的具体实现
└── GMae-24GAS / GMae-48GAS … — 未来按显存规模扩展的落地项目编号
```
> **注**：Prism Engine（P-Eng）不是独立层级——它是 vram-console 承载的核心调度引擎的概念名（"棱镜分光"：一张卡拆成多模态能力）。

### 解决的痛点
消费级 GPU（16GB 显存）无法同时运行多模态大模型（LLM + 文生图 + 文生视频 + 文生音乐），用户被迫在多个工具间手动切换、频繁重启容器，显存打满即死机。高端算力方案（多卡/云GPU）成本高昂，个人开发者和中小团队难以承受。

### 核心创新
**Prism Engine-棱镜引擎（P-Eng）** — 面向单卡多模态的智能显存调度系统：
- 将一块物理 GPU 通过软件调度"拆分"为多种模态能力（对话/图/视频/音乐），如棱镜分光
- 动态显存预算 + 分级释放机制 + 任务串行队列，确保 16GB 单卡稳定运行全模态 AI
- 场景化一键切换 + 服务质量主动编排，用户无需关心底层显存管理
- 配置驱动架构，兼容模型升级和硬件扩展（16GB→24GB→48GB→多卡）

### 大赛契合点（智算云赛道）
| 赛道关键词 | 本项目对应 |
|-----------|-----------|
| 异构算力调度 | 单卡内多模型/多引擎的错峰调度与主动编排 |
| GPU 池化 | 将单卡显存虚拟化为可分配资源池，按任务动态分配 |
| 推理加速 | 模型级秒级释放（/free）替代容器级重启（10-30秒），缓存复用减少重复加载 |
| 边云协同 | 面向边缘端（消费级显卡）的轻量化调度，未来可扩展云端协同 |
| 产业价值 | 降低多模态 AI 使用门槛，个人开发者/中小团队用 16GB 卡即可构建全模态 AI 工作站 |

### 赛道原文与参赛策略（2026-08-27 补充）

**官方原文（智算云赛道，报名页原话）**：
> 本赛道面向用云来加速、简化或稳定 AI 运行的开源项目，从底层异构算力调度、GPU 池化、分布式训练，到上层 AI 应用开发平台、模型部署服务、Serverless 推理、工作流编排……不区分基础设施或应用层，只要通过云原生技术解决了 AI 开发、部署或运维中的实际痛点，无论是为千卡集群减负，还是为开发者提供一键部署的 AI 应用环境，均可参赛。

**官方社区解读补充（OSC 开源社区，2026-08-25）**：
> 只要参赛项目与云原生技术相关，或者提升了 AI 应用的开发、部署、运行效率，又或者降低了 AI 的使用成本……哪怕只是解决一个具体的小痛点，都在征集范围内。
> 评审尤其关注三个方面：实际痛点是否真实，技术方案是否落地，有没有可量化的效果（即利用率、成本和部署时间）。

**下沉叙事（赛道对齐策略）**：官方语境偏"云/集群"（用云、云原生、千卡集群、异构 GPU），本项目是本地消费级单卡显存编排。对齐方式 = **下沉投影**——把官方每一个"云级"关键词投影到"消费级单卡"：
- "为千卡集群减负" → "为一张 16GB 消费级显卡减负"：同一个痛点（算力利用率低、资源空转浪费），数据中心已被 K8s/Slurm 解决，**消费级是真空**
- "GPU 池化" → "单卡显存池化"（物理显存虚拟化为可分配资源池）
- "算力调度" → "单卡内多模型错峰调度 + 主动编排"
- "可量化效果" → 16GAS 实测数据：`/free` 秒级释放（vs 容器重启 10-30 秒）、峰值显存实测、场景切换耗时、零 OOM
- 一句话：**大厂/社区都在解决"数据中心算力浪费"，没人解决"消费级单卡算力浪费"——GMae 填补后者**

**开源治理准备清单（评审 20%，当前短板，必做）**：
| 项 | 现状 | 动作 | 节点 |
|----|------|------|------|
| 开源协议 | 无 | 定 Apache-2.0（或 MIT） | 提交前 |
| 公开仓库 | 私人 | GitHub/Gitee 建 `GMae-16GAS` 仓库并同步代码 | 提交前 |
| README | 无 | 项目定位/架构/快速开始/截图 | 提交前 |
| 治理文档 | 无 | CONTRIBUTING + Issue/PR 模板 | 提交前 |
| 依赖合规 | 需核查 | 核对 Docker/ComfyUI 生态第三方依赖协议 | 提交前 |
| 治理意识 | 无 | README 体现规划与治理思路（新项目重点考察"规划与治理意识"） | 提交前 |

**评审维度对照（官方原话）**：
| 维度 | 权重 | 我们的策略 |
|------|------|-----------|
| 技术创新 | 30% | P-Eng 完整编排层 vs 社区碎片工具（OneGPU4All 等），明确技术优势 |
| 场景落地 | 30% | 16GAS 真实运行实测数据（可部署可运行可测试） |
| 开源治理 | 20% | 按上表补协议/仓库/文档/治理 |
| 长期发展 | 20% | GMae-XXGAS 产品矩阵规划 + 跨 OS 演进路线 + 个人故事与 AI 协作叙事 |

### 叙事钩子（个人故事 + AI 协作，2026-08-27 定稿）

**一句话开场（作品介绍/路演钩子）**：
> 一个只学过简单 C++ 的文科生，靠多个 AI Agent 协作（豆包 / Deepseek Harness / WorkBuddy / Qclaw / Openclaw），完成的首个开源项目——GPU Maestro，旨在解决所有消费级 AI 显卡的算力编排问题。

**叙事三要素**：
| 要素 | 内容 | 打的评审点 |
|------|------|-----------|
| 跨界 | 文科生 + 只学过简单 C++ | "开源向实"普惠精神，评委易共情 |
| 协作范式 | 一人 + 多 Agent（AI 承担编码与验证） | 2026 时代叙事，现代开发范式 |
| 真痛点 | 消费级显卡算力浪费（官方数据：智算算力使用占比仅 37%，70% 空转） | 技术创新/场景落地的初心 |

**披露边界（硬规则）**：架构、机制、工程决策（P-Eng 的门卫/指挥家/预算引擎/登记台等）为**人类原创设计**，AI Agent 承担编码与验证——"技术创新 30%"的原创性在**设计层**，不因 AI 代笔而稀释。作品介绍在「项目开发」部分明示：*本项目由一人 + 多 Agent 协作开发，AI 承担编码与验证，架构与产品决策由人类主导*。

### 战略定位：消费级单卡显存编排层（2026-08-27 确认，跨 OS 演进）

**GPU Maestro（GMae）到底要做什么？** — 面向**所有消费级 AI 服务器**的**显存编排层**：把一块（或一套）被所有人忽略的"算力孤岛"变成可被高效、可靠、无痛编排的本地多模态工作站。它不是又一个 AI 生成工具（那拼不过大厂），它是管"AI 生成工具怎么在这块卡上活得好"的那一层。**不限于 16GB**——8G/12G/16G/24G/48G 等消费级配置都在目标范围。

**命名体系（2026-08-27 定稿）**：产品总名 = **GPU Maestro（GMae）**；落地项目按显存规模编号——`GMae-16GAS`（= GPU Maestro-16G-AI-Studio，16GB 单卡，大赛投稿项目）、未来 `GMae-24GAS` / `GMae-48GAS`…。**16GAS 是"落地项目编号"，不是产品名**。

**范围边界（2026-08-27 确认）**：**先单卡**（消费级 8G~48G，预算引擎动态读显存总量）；**多卡作为 P4**（预留 `gpu_id` 字段，暂不实现）。

**市场分层与边界定义（2026-08-27 确认）**：GMae 的边界**不是按操作系统切，而是按"单卡内编排（intra-GPU）vs 跨卡调度（inter-GPU）"切**。

**算力编排金字塔**：
| 层级 | 场景 | 现状 | 代表 | 视角 |
|------|------|------|------|------|
| 集群层 | 数据中心/多机多卡 | **成熟饱和**（红海，绝不进入） | K8s GPU Operator + DRA、Slurm、HAMi、Volcano、Slinky | 集群管理员 |
| 单机多卡层 | 1-8 卡工作站 | **半空白**（无"多模型共存编排"） | Ray、GPU Task Spooler、OrionX/MTools（企业 vGPU） | 研究员/管理员 |
| 消费级单卡层 | 8G-48G 个人机 | **空白/碎片（我们的主场）** | OneGPU4All（已跨平台）、手动 nvidia-smi 脚本 | 创作者/个人 |

**关键洞察**：
- 集群层"成熟"的是**跨卡/多任务调度（inter-GPU）**——"100 个任务怎么分给 100 张卡"（配额/公平性/拓扑感知），**不是单卡内部多模型共存（intra-GPU）**。后者无论 Windows 还是 Linux、单卡还是单机多卡，都无人做全。
- **往上撞必死**（K8s/Slurm 巨头红海）；**守住并做透"一张卡内部，AI 工具怎么活得好"这一层**。

**边界定义**：
```
我们管（intra-GPU）：一张卡内部，多个模型/服务如何共存、切换、腾挪、保活
我们不管（inter-GPU）：多卡间任务分配、多租户配额、集群调度 ← K8s/Slurm 的天下
```

- **OS**：当前 Windows 做透（16GAS 落地）；**Linux 是明确演进方向**（非"要不要"，是"什么时候"）——架构保留"平台适配层"口子（感知层解析 nvidia-smi 平台差异、进程治理器抽象 Windows taskkill / Linux cgroup）
- **显存**：消费级 8G~48G，不碰数据中心专业调度
- **多卡**：P4 预留 `gpu_id`，未来可向上延伸"单机多卡"（每卡一实例），单卡做透之后再议

**升级后叙事**：算力编排金字塔，顶层（数据中心）已被 K8s/Slurm 巨头占据，底层（消费级单卡、无论 Windows/Linux）碎片化真空——GMae 填补"一张卡内部，AI 工具怎么活得好"这一层。**跨平台是明确演进方向，先守住单卡主场**。

**同类格局（2026-08-27 调研确认）：平台层空白 + 社区层碎片化，无统治性产品**

**平台层缺口（为什么大厂没玩明白）**：
| 玩家 | 做了什么 | 缺口 |
|------|---------|------|
| NVIDIA（vGPU/MIG） | 面向数据中心，企业级授权 | 消费级卡不支持 |
| 云 GPU 平台 | 按小时租算力 | 不是本地创作工作站 |
| ComfyUI | 只做工作流执行 | 不做显存编排，多模型共存即 OOM |
| Ollama | 只管 LLM | 不管图/视频/音乐 |
| K8s + GPU Operator | 面向集群 | 个人单卡用不上 |
| Windows 本身 | 零机制，GPU 资源"谁抢到是谁的" | **平台层空白核心** |

**社区层：同类工具正在密集涌现（2026-04~08），但全部是碎片工具**
| 工具 | 发布 | 覆盖 | 缺什么 |
|------|------|------|--------|
| OneGPU4All（Node.js） | 2026-08 | 单卡 VRAM broker：代理+排队+让占用者卸载+新后端加载；Ollama/ComfyUI 卸载 | 无预算算账、无场景、无门卫、无登记台、无 QoS、无 Web 控制台 |
| Model Manager（Python） | 2026-01 | 模型 broker：队列+VRAM 调度+模型注册表+资源监控 | 只做 LLM 路由；不碰图/视频/音乐；无 Web UI |
| Ollama Tray Guard（Go） | 2026-04 | Windows 托盘：检测非 Ollama 占用→自动卸载+toast | 只管 Ollama 一个点 |
| gpu-orchestrator（Node） | 2026-05 | doctor/optimize/monitor：生成 Ollama/llama.cpp/vLLM 显存上限配置 | 明确"只建议不杀进程"，不主动执行 |
| StageHand / SynapSwap | 2026-08 | PyTorch 模型 offload（RAM↔VRAM 流式） | 单模型扩容路线，非多模型编排 |
| VRAM-Pressure-Scheduling | 2026-08 | 多 GPU 调度理论（学术向） | 无产品化 |
| HAMi / k8s-vgpu | 成熟 | 集群级 vGPU 共享（K8s 场景） | 企业集群场景，个人单卡用不上 |

**结论：没有一个做到"完整显存编排层"**。碎片工具各自只解决一块——OneGPU4All 抢"VRAM broker"心智（只解决冲突），但**门卫（进程治理/驱逐）、指挥家（QoS/回收/队列）、模型登记台、预算算账、多模态全栈、Web 指挥台**没有任何一个做全。

**GMae 差异化锚点**：不是又一个 broker，而是**完整的单卡显存编排层**——broker 只解决"冲突"，编排解决"活得好"。护城河 = 全栈覆盖 + 实际运行验证（16GAS 实测）+ Web 指挥台 + 开放 API + 配置驱动。

**叙事修正（替代"大厂没做"的说法）**：大厂没做 + **独立开发者正在涌现 + 尚无统治性产品 + 碎片化严重** → GMae 以"完整编排层"切入并做到极致。

**从同类工具吸收的启示**：
1. **智能路由（OneGPU4All）**：区分"GPU 请求"（推理/生成）vs"passthrough 请求"（UI 轮询、模型列表、健康检查）——浏览器轮询不应触发显存切换。**吸收为 Step 8 任务队列的可选增强（代理模式，P3+ 评估）**
2. **软上限不杀进程（gpu-orchestrator）**：与我们"桌面应用永远只 L0 warn"哲学一致，互相印证
3. **Windows toast 实践（Ollama Tray Guard）**：托盘+通知体验可借鉴，我们已规划桌面 toast

**目标用户聚焦（专家优先）**：本地 AI 创作者、自部署者、ComfyUI 用户。他们需要"忘掉显存的存在"：不卡脖子（自动腾挪）、不踩坑（预警/排他/不死机）、高效（批量队列）、透明可编程（进程级可视化 + 完整 API）、低摩擦（模型扫描器一键登记）。

**小白链路（OWUI → LLM 工具调用 → ComfyUI 一条龙）明确搁置为远期**。理由："帮我生成5秒赛博上海夜景"是伪需求——创作是多轮迭代，不是一句话点单；且 9B 模型工具调用可靠性未验证（模型能力问题，非工程可解）。当前不做，也不为它分心。

**对设计的影晌**：全部 Step 0-8 围绕"专家显存编排极致"；交互界面以"透明可编程"面为主，"隐身魔法"面（小白一句话出片）降级为远期；大赛叙事从"产品噱头"转向"工程深度"。

---

## 二、核心设计原则

所有设计决策必须遵循以下 5 条，按优先级排序：

### 原则 1：任务完成优先（可靠性）
- 宁可降级、宁可慢，不能让任务失败或系统死机
- 执行前严格显存预检，绝不"试试看"
- 失败有降级、有重试、有明确错误反馈
- 看门狗保障服务不中断

### 原则 2：效率与反应速度最大化
- 优先模型级释放（秒级），不轻易停容器（10-30秒）
- 已加载模型能复用就复用，不重复加载
- 异步执行 + 后台预热，减少用户感知等待
- 缓存策略：对话模型保留 30 分钟，生成模型用完即卸

### 原则 3：用户友好（界面即产品）
- **界面是开源大赛成败的关键**——把抽象的显存调度翻译成用户秒懂的故事
- 等待过程必须有进度反馈（排队中/加载中/生成中/百分比/预计时间）
- 状态描述用自然语言，不暴露技术术语；调度决策透明化（用户知道系统在协调什么、为什么）
- 完成后主动告知结果和耗时；异常时给出可操作的建议

### 原则 4：可扩展性
- 所有可变资源（模型、场景、工作流）放配置文件，代码只处理逻辑
- **模型（"乐手"）经常更换，必须有便捷的更新机制**（模型登记台）
- 硬件参数动态读取，不硬编码 16GB；执行引擎可插拔；多卡预留扩展位

### 原则 5：可维护性
- 统一结构化日志，所有操作可追溯；健康检查一键诊断
- 崩溃自动重启 + 记录原因；模块边界清晰，改一处不影响全局

---

## 三、总体架构：从门卫到指挥家

### 3.1 定位演进（2026-08-27 确认）

调度中心的角色必须经历三个阶段，这是本设计的主线：

```
阶段1 遥控器+监视器（当前）── 被动：切场景、看总量、被问才动
    ↓ 升级：准入制管理
阶段2 门卫 ── 防御：登记簿白名单 + 未登记驱逐 + 警告通知
    ↓ 升级：主动编排
阶段3 指挥家 ── 编排：安全约束下最大化吞吐和体验（QoS/回收/队列）
```

| 角色 | 目标函数 | 典型动作 |
|------|---------|---------|
| 遥控器 | 别超显存、别白占 | 切场景、显示总量、health |
| 门卫 | **安全**（准入、驱逐、告知） | 登记簿、踢出未登记、警告 |
| 指挥家 | **安全约束下最大化吞吐和体验** | 主动降级/升档、空闲回收、任务编排 |

**关键洞察**：指挥家能"主动"的前提是**有耳朵**——能感知每个服务在干什么（ComfyUI 任务事件、服务活跃度）。感知层是全部机制的地基。

### 3.2 三层架构

```
┌────────────────────────────────────────────────────────────┐
│                    用户界面层（指挥台）                       │
│  日常仪表盘(看) / 动作大按钮(点) / 设置折叠层(调)           │
└─────────────────────────┬──────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────┐
│              调度中心（P-Eng 核心，Python 后端）              │
│                                                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │  感知层（耳） │  │  账本层（脑）  │  │  执行层（手）     │  │
│  │ 进程级显存    │  │ 预算引擎      │  │ 分级释放 L1-L4    │  │
│  │ ComfyUI事件  │  │ 服务登记簿    │  │ QoS 降级/升档     │  │
│  │ 服务活跃度    │  │ QoS 状态机    │  │ Idle Reaper      │  │
│  │              │  │ 模型登记台    │  │ 任务队列编排      │  │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
│                                                            │
│  基础设施：日志 / 健康检查 / 看门狗 / ThreadingHTTPServer    │
│  / WebSocket 客户端 / 桌面通知（toast）                      │
└─────────────────────────┬──────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────────┐
│                   执行引擎层（乐手）                         │
│  Ollama API  │  ComfyUI API(+/free)  │  Fooocus  │ [扩展]  │
└────────────────────────────────────────────────────────────┘
```

### 3.3 模块职责总览

| 模块 | 层 | 职责 | 不负责 |
|------|-----|------|--------|
| 进程级感知 | 感知 | `nvidia-smi --query-compute-apps` 读每个进程显存 | 管理 |
| ComfyUI 事件 | 感知 | WebSocket 订阅任务/队列变化（指挥家的耳朵） | 执行 |
| 服务活跃度 | 感知 | 记录各服务最后请求时间（Idle Reaper 依据） | 执行 |
| 资源登记 | 账本 | registry.json 单一数据源 + gpu_guard 登记簿 | 实际执行 |
| 模型登记台 | 账本 | 模型增删改查：扫描器 + 估算 + 试跑校准 | 调通工作流 |
| 预算引擎 | 账本 | 任务前算账，判断能否执行、差多少、该腾谁 | 场景定义 |
| QoS 状态机 | 账本 | 显存水位 → 服务等级 → 降级/升档决策 | 执行 |
| 分级释放 | 执行 | L1 停模型 / L2 /free / L3 重启容器 / L4 告警 | 场景定义 |
| 驱逐器 | 执行 | 未登记进程：警告 / 询问 / 踢出 | 判定 |
| 场景管理 | 执行 | 场景定义 + 切换 | 任务级调度 |
| 任务队列 | 执行 | 串行编排生成任务（终态） | 显存管理 |
| 基础设施 | — | 日志、健康、看门狗、通知、配置 | 业务逻辑 |

---

## 四、服务登记簿（gpu_guard）与驱逐策略 — 门卫

### 4.1 设计思想
调度中心无法"禁入"未登记的 GPU 进程（Windows 无 cgroup 类机制），但可以做到：
**服务启动工作台（所有受管服务从调度中心启停）+ 服务登记簿（白名单）+ 未登记检测 → 驱逐 + 警告通知**。

### 4.2 登记簿结构（registry.json 扩展）

```json
"gpu_guard": {
  "managed": [
    {"proc": "python.exe", "path_contains": "comfyui", "note": "ComfyUI 容器"},
    {"proc": "python.exe", "path_contains": "fooocus", "note": "Fooocus 容器"},
    {"proc": "ollama.exe", "note": "Ollama"},
    {"proc": "whisper-cpp.exe", "note": "TTS/STT"}
  ],
  "protect": [
    {"proc": "dwm.exe", "note": "桌面窗口管理，杀=黑屏"},
    {"proc": "explorer.exe", "note": "系统外壳"}
  ],
  "unknown_policy": "warn"
}
```

- **managed**：受管服务，调度中心可停（docker stop / API）
- **protect**：系统进程，永不触碰，出现即忽略
- **unknown**：未登记，命中驱逐策略

### 4.3 驱逐策略（三级，默认 L0）

| 级别 | 动作 | 适用 |
|------|------|------|
| L0 warn | 横幅 + 日志 + 桌面 toast"未登记进程 X 占用 Y GB" | 默认，安全 |
| L1 warn+ask | 前端弹确认"是否踢出？" | 用户在场时 |
| L2 warn+kick | 直接杀 + 通知 | **仅对可安全重启的服务**（Fooocus 残留、whisper 残留） |

**强制边界（不可突破）**：
1. 踢之前必须**验明正身**——比对可执行文件完整路径 + 命令行参数，不能只匹配进程名（同名 ≠ 同一程序）
2. **桌面应用（浏览器/微信/WPS）永远只 L0 warn**，绝不自动杀——杀浏览器会丢用户工作
3. **protect 类进程永不触碰**
4. "驱逐"是**事后执法**（最快下一轮轮询 3 秒才看见），不是事前拦截——明确此定位

### 4.4 与场景切换联动（门卫的主动版）
场景切换时对比"本场景应有进程 vs 实际进程"：例如切回对话态，发现 Fooocus 的 python 还赖着 → 自动踢 + 通知。这是"Fooocus 常驻显存"痛点的自动化解法（R6 从人记变自动）。

---

## 五、感知层设计（指挥家的耳朵）

### 5.1 数据源清单

| 数据 | 来源 | 状态 | 用途 |
|------|------|------|------|
| 显存总量 | nvidia-smi | ✅ 已有 | 总水位 |
| **进程级显存**（每 PID 占多少） | `nvidia-smi --query-compute-apps` | ❌ 待接 | 谁占了多少（门卫 + 预算真相） |
| ComfyUI 队列 | `GET /queue` | ❌ 待接 | 正在跑/排队 |
| ComfyUI 加载模型 | `GET /history` | ⚠️ 现有方案A（推断） | 降级为辅助 |
| **ComfyUI 实时事件** | `WebSocket /ws` | ❌ 待接 | 任务进度/完成（队列前提） |
| Ollama 加载模型 | `GET /api/ps` | ✅ 已有 | 对话模型状态 |
| 服务活跃度 | 内部记录最后请求时间 | ❌ 待建 | Idle Reaper 依据 |

**关键**：`nvidia-smi --query-compute-apps` 是"谁占了多少显存"的唯一真相来源，现在一行都没读。接上它，前端分段显存条就从"估算"变"实测"。

### 5.2 服务活跃度追踪
每个服务记录"最后活跃时间"（收到请求 / 任务事件时刷新），供 Idle Reaper 判定空闲。

---

## 六、显存预算引擎（P2）

### 6.1 预算公式

```
avail = total − 系统底噪(≈1G) − 保留值(2.5G) − 其他进程占用(compute-apps 中非已知服务的)
能跑 = 请求模型标称显存 + 当前已用 ≤ total − reserve
```

### 6.2 核算动作（四选一）

| 判定 | 动作 |
|------|------|
| ok | 直接执行（利用已有缓存） |
| free_L1 | 停 Ollama 非保留模型 |
| free_L2 | ComfyUI `/free` 卸载当前模型 |
| reject | 连释放都不够 → 明确告知差多少 GB |

### 6.3 标称值校准
registry.json 已有 `vram_gb`（标称），新增 `measured_vram_gb`（试跑实测峰值）。Flux Q5 标称 13G、实测 11.7G 这类差异，结构化记录后预算才准。

### 6.4 独占规则（硬约束）
`exclusive: true` 的模型（27B/Flux/Wan2.2/Music3）加载前必须释放所有其他 GPU 模型——违反 = 显存打满 = 死机（历史教训），预算引擎强制执行，不可绕过。

### 6.5 分级释放（L1-L4）

| 级别 | 操作 | 耗时 | 副作用 | 使用时机 |
|------|------|------|--------|---------|
| L1 | Ollama stop / keep_alive=0 | 秒级 | 模型重载 | 常规 |
| **L2** | **ComfyUI `POST /free`** | 秒级 | 模型重载 | 常规（灵魂机制） |
| L3 | docker restart 容器 | 10-30秒 | 容器状态丢失 | 兜底 |
| L4 | 告警"进程 X 占 Y GB 请手动关闭" | — | 无 | 桌面应用 |

**原则**：优先 L1/L2（模型级、秒级），L3 仅兜底，L4 只对桌面应用。

---

## 七、QoS 服务质量引擎（指挥家第一个主动动作）

### 7.1 设计思想
把"显存水位"翻译成"用户体验"：不是死板地拒绝，而是**用最省的方式继续提供服务，并让所有人知道发生了什么**。

### 7.2 水位状态机

| 水位 | 对话服务 | 动作 |
|------|---------|------|
| GREEN < 8G | 9B 全量 | 正常 |
| YELLOW 8~12G | 保持 9B | 提示"建议轻量模式" |
| RED > 12G | **自动降 0.6B** | 通知"服务正忙，聊天已降级轻量模型，响应更快但质量下降" |

### 7.3 用户知情三路齐发
- 调度中心前端横幅（用户守指挥台时）
- **桌面 toast**（Windows 原生通知，Python 调 PowerShell UWP Notification）
- 操作日志

**边界**：OWUI 是独立容器，调度中心无法直接注入其内部横幅；通过"调度中心仪表盘 + 桌面通知"让用户知情，不强行改造 OWUI。

---

## 八、Idle Reaper 空闲回收（指挥家主动清扫）

把"用毕必停"（R6）从"靠人记"变成"系统自动"：

| 资源 | 空闲阈值 | 动作 |
|------|---------|------|
| ComfyUI | N 分钟 | 自动 `/free` 卸载模型 |
| Fooocus | N 分钟 | 自动停容器 |
| Ollama | 已有 keep_alive=30m | 顺势统一管 |

配合服务活跃度追踪（5.2），这是"门卫驱逐"的主动版——用户不用再手动盯"Fooocus 常驻显存"。

---

## 九、任务队列模块（指挥家终态）

### 9.1 为什么需要
16GB 单卡**物理上不能并发**两个生成任务，必须串行化。队列把"手动三步（提交→等→腾显存→再提交）"自动化。

### 9.2 解决的场景
- **批量出素材**：一次丢 5 个任务（视频+图），自动逐个执行、逐个腾挪、自动收尾
- **无人值守/错峰**：睡前排队跑 4 个视频，早上看结果
- **对话插队**：生成任务占用时，对话请求降级/暂停，保证对话不卡（与 QoS 联动）

### 9.3 前置依赖（必须满足，否则队列是"瞎"的）
1. **WebSocket 事件源**——调度中心必须知道"任务何时跑完"，才能自动开始下一个
2. **工作流参数化**——预先知道"跑视频 = 哪个模板 + 什么参数 + 需要多少显存"（registry 的 workflow 字段）
3. **任务路由器**——队列混有 ComfyUI/Fooocus/Ollama 任务，能调起不同后端

### 9.4 特性
- 串行执行、可取消、可观察（状态/进度/预计剩余）、去重
- 状态机：提交 → 排队 → 预检 → 释放 → 加载 → 生成 → 完成（含失败降级/重试）

### 9.5 诚实优先级
队列价值取决于使用习惯：**经常批量/无人值守才值得做**。大部分时间一次一两个任务 + 愿意盯着 → 边际价值低。建议放最后，QoS + Reaper 先见效。

---

## 十、模型登记台（便捷换乐手机制）

### 10.1 现状痛点
一个模型现在散落三处硬编码：registry.json（后端账本）+ index.html 的 `BUDGET_ROWS`/`SCENES`（前端显示，**写死的**）+ 工作流模板。换一个模型要改 2-3 个文件，易出错、不易维护。

### 10.2 核心：登记一处，全局生效
1. registry.json 保持唯一登记处（扩展 `measured_vram_gb` / `enabled` / `family` 等字段）
2. **前端去硬编码**：BUDGET_ROWS、SCENES 全部改为从 `/api/status` 返回的 registry 数据**动态生成**。改完这个，**加模型 = 只动 registry 一个文件，前端自动多一行**——这是"便捷"的地基

### 10.3 模型扫描器（"经常换"场景真正想要的）
- 定期扫描各模型目录（ComfyUI checkpoints、Fooocus、Ollama /api/tags）
- 发现新文件 → 前端提示"检测到新模型 X（大小/疑似家族/量化），是否登记？"
- 一键确认 → 按"文件大小 + 量化类型 + 家族"**自动估算显存** → 写入 registry → 前端立即出现
- 换量化版本 = 扫描器发现新文件，选"替换现有模型"

### 10.4 试跑校准
登记时可选"试跑校准"：提交最小工作流（如 256×256 测试图），实测显存峰值 → 自动回写 `measured_vram_gb` → 标记"已校准"。账本数字是实测不是手估。

### 10.5 三种换乐手情形

| 情形 | 动作 | 工作量 |
|------|------|--------|
| 换量化版（Q5→Q8） | 扫描器发现 + 选"替换"，工作流沿用 | 几秒 |
| 新增家族（新下载视频模型） | 登记 + 配工作流模板 | 主要在调通工作流本身 |
| 停用（H3 已废） | `enabled: false`，前端灰显、不参与调度 | 一键 |

### 10.6 边界
**便捷登记 ≠ 自动跑通**。调度中心自动化"发现→登记→估算显存→校准"，但新模型能出活的关键仍是 ComfyUI 工作流调通（节点兼容、CLIP/VAE 配置）——那是模型特有的手艺活，自动化帮不了。调度中心管"乐手进名册、占多大位置、何时进场"，不管"乐手会不会吹"。

---

## 十一、人机交互界面设计（大赛成功关键）

> **定位**：界面是把"显存调度"这个抽象技术翻译成用户秒懂的故事。开源大赛评创新 + 工程 + 可用性，界面是可用性的直接体现。
> **设计哲学（2026-08-27 定稿）**：**少即是多——默认不打扰，接管能力永远在手边**。系统越聪明，界面越简单：复杂度全部藏在引擎里，界面上只留用户真正需要的三件事——**看得懂、能否决、能接管**。
> **主面**：面向专家（玩家）的"透明可编程"界面——完整 API、进程级可视化、预演模式。"隐身魔法"面（小白一句话出片）**降级为远期**，不作为当前设计目标。

### 11.1 设计理念：少即是多 + 指挥家隐喻

**核心判断：**
- 自由度 ≠ 让用户手动管理一切；自由度 = 用户对系统每个决定的**可否决权 + 可接管 + 可回看**
- **操作 ≠ 配置**：操作是"当场点一下"（直觉、面向当下）；配置是"事先设规则"（烧脑、面向未来）。界面**优先给操作，配置收进折叠层**——之前"八个授权开关"错在把操作做成了配置
- **系统学习用户，不是用户配置系统**：默认全自动，系统自己判断什么事值得问；判断标准只有一条——**这个动作会造成不可逆/有体验损失吗？** 是 → 弹一次确认；不是 → 直接做。用户答一次（"这次同意 / 以后这类都自动"）就记住，授权自动收敛，界面配置项趋近于零

指挥家隐喻（贯穿用词，保持简单）：

| 产品概念 | 界面隐喻 |
|---------|---------|
| GPU 显存 | 舞台/乐池（能量可视化） |
| 模型 | 乐手（卡片带图标） |
| 调度决策 | 指挥棒动作（状态播报） |

### 11.2 三层界面结构（一个界面按需展开）

```
第一层 日常（默认，只读）—— 仪表盘
   GPU 显存能量条 + 各乐手状态 + 健康度 + 门卫横幅
   三秒看懂"系统忙不忙、谁在干活"；演出日志（显存曲线）可在此展开
   看一眼就走，不用动

第二层 动作（想动手时，3-5 个大按钮）—— 操作，点一下就行
   [一键全释放] [切换场景] [预演模式]
   预演模式（大赛演示杀手锏）：点"我要跑 Flux 顶格视频"→ 先显示
   "将释放 Fooocus、对话降级 0.6B、预计 4 分钟"→ 确认才挥棒，先看谱再指挥

第三层 设置（想调优时，默认折叠）—— 配置，平时看不见
   高级设置入口：让路名单、QoS 策略、档位、模型登记台
   能力都在，但收在折叠层，不打扰
```

**浅处浅得干净，深处深得完整。** 日常只碰第一层，忙的时候用第二层，调优才进第三层——能力一个不少，默认界面只有"看一眼 + 点一下"。

### 11.3 自由度尺度（怎么拿捏）

| 时机 | 尺度 |
|------|------|
| 日常 | 全自动，界面只读，不打扰 |
| 关键动作（不可逆/有体验损失） | 弹一次确认，答一次记住，下次同类自动 |
| 极端时刻 | **Panic 大按钮永远可见**：一把全释放、立即停 |
| 想深入 | 第三层折叠设置，随时可调 |

**兜底原则**：宁可系统保守（该问时问一次），不赌用户想要什么；桌面应用/前台游戏永不自动杀（L0 封顶）。

### 11.4 设计原则
1. **状态 > 数字**：优先颜色/动效/自然语言，GB 数字降到细节层
2. **状态播报**：调度动作转成一句话（"正在为 Flux 腾出 13G，对话暂时降级…"）——用户看见系统在"想"
3. **一屏一主题**：默认只给仪表盘，深入才展开，不堆卡片
4. **克制的设计语言**：深色科技主题（现有基础）+ 品牌标识（GMae）+ 图标 + 动效

### 11.5 诚实提醒（边界）
1. **界面是最后一公里，不是地基**：门卫/指挥家机制才是 Demo 要"演示的内容"，界面只是把它们"演出来"。只做壳不做机制 = 空壳演示
2. **界面必须与后端机制同步长**：每上线一版机制，界面就多一个能"讲"的点
3. **开源属性**：还要考虑"别人 fork 后看到什么"——空状态设计、默认配置、文档入口

---

## 十二、基础设施层设计（壳升级）

### 12.1 架构升级三步（2026-08-27 确认，页面是脸，Python 后端是脑子和手）

| 升级 | 内容 | 说明 |
|------|------|------|
| ① ThreadingHTTPServer | server.py 单线程 → 多线程 | 标准库改一行，防慢请求阻塞 |
| ② WebSocket 客户端 | 连 ComfyUI `/ws` 订阅事件 | 指挥家的"耳朵"（优先于服务端 WebSocket） |
| ③ 桌面 toast | Python 调 PowerShell UWP Notification | 通知不依赖页面开着，三路齐发 |

### 12.2 统一日志
- 位置 `logs/vram-console-YYYYMMDD.log`，结构化 JSON，保留 30 天
- 必填：时间戳、事件类型、操作、结果、耗时、显存快照

### 12.3 健康检查
- `/api/health`：调度中心自身 + Ollama/ComfyUI 连通 + 显存阈值 + 队列长度

### 12.4 看门狗
- 崩溃 5 秒自动重启，记录原因；连续 3 次暂停重启并告警；登记在 WATCHDOGS.md

---

## 十三、扩展性设计

### 13.1 模型扩展（模型登记台）
- registry.json 加一行 + workflows 加文件 → 前端自动识别（去硬编码后）
- **无需改代码**（P2.5 完成后）

### 13.2 硬件扩展
- 显存总量从 nvidia-smi 动态读取；换 24GB/48GB 卡预算自动调整（单卡优先，消费级 8G~48G）
- 多卡：registry 支持 `gpu_id` 字段（**P4 预留，暂不实现**）

### 13.3 执行引擎扩展
- 抽象"执行引擎"接口（提交/查询/取消/取结果）；ComfyUI、Fooocus、新引擎各自实现

### 13.4 场景扩展
- scenes 定义加一条 → UI 自动出现按钮，无需改代码

---

## 十四、实施路线图（Step 0-8）

| Step | 内容 | 产出/验证 | 角色 |
|------|------|----------|------|
| **Step 0** | 壳升级：ThreadingHTTPServer + 桌面 toast | 并发请求不阻塞、通知三路齐发 | 基础设施 |
| **Step 1** | ComfyUI `/free` 打通 | curl /free 前后显存秒降（实测验证） | 执行层 |
| **Step 2** | 进程级感知（compute-apps） | /api/status 多 `procs` 字段，前端分段变实测 | 感知层 |
| **Step 2.5** | 服务登记簿 gpu_guard + 三级驱逐 | 门卫雏形：未登记检测 → warn/kick | 门卫 |
| **Step 3** | 模型登记台：前端去硬编码 + 扫描器 + 试跑校准 | 加模型只动 registry，前端自动多行 | 地基 |
| **Step 4** | 预算引擎核算 + /api/budget | 能算出"Flux 顶格能不能跑、差多少" | 账本层 |
| **Step 5** | QoS 服务等级引擎 | RED 水位自动降 0.6B + 通知 | 指挥家 |
| **Step 6** | Idle Reaper 空闲回收 | ComfyUI/Fooocus 空闲自动卸载 | 指挥家 |
| **Step 7** | ComfyUI WebSocket 事件源 | 实时任务进度/完成（队列前提） | 感知层 |
| **Step 8** | 任务队列（终态，可选） | 批量/无人值守流水线 | 指挥家终态 |
| **Step 8+** | 代理式智能路由（可选增强） | 调度中心作代理，区分 GPU 请求 vs passthrough，UI 轮询不触发 swap | 吸收自 OneGPU4All |

**关键顺序逻辑**：感知（Step 2）→ 门卫（2.5）→ 地基（3）→ 账本（4）→ 指挥家（5/6）→ 耳朵（7）→ 终态（8）。每步独立可验证，不做"写了再说"的纸面功能。

**战略优先级（2026-08-27 确认）**：全部 Step 围绕"专家显存编排极致"——感知/门卫/登记台/预算/QoS/Reaper/WebSocket/队列。这是 GPU Maestro（GMae）的核心价值（消费级 AI 服务器显存编排层），也是开源大赛的工程深度叙事。

### 与旧 P0-P5 阶段的对应
- P0（稳定性）✅ 完成
- P1（registry 配置驱动）✅ 完成
- P2 = Step 1/2/4（/free + 感知 + 预算）
- P2.5 = Step 2.5/3（登记簿 + 模型登记台）
- P3 = Step 7/8（WebSocket + 任务队列）
- P4 = OWUI Function Calling（**小白一条龙链路，2026-08-27 已搁置为远期**）；P5 = 预热/降级（沿用原规划）

---

## 十五、与现有文档的关系

| 文档 | 关系 |
|------|------|
| 《显存管理最高指南》v1.3 | **最高指导**，如有冲突以最高指南为准 |
| 调度中心 README.md | 操作手册，本文档是设计依据 |
| ops-changelog.md | 变更记录，每次实施后更新 |
| 产品化子项目方案 | 本文档是产品化的技术底座 |
| 项目进度跟踪.md | 里程碑与任务状态（本文档设计的实施状态） |
| 开发日志.md | 踩坑记录（实施时同步） |

---

## 十六、开放问题（待决策）

1. **隐式场景切换**：用户从 OWUI 直接出图时，是否自动切换场景？（建议 P4 再做）
2. **任务持久化**：调度中心重启后未完成任务是否恢复？（建议队列成熟后再议）
3. **多用户**：16G 卡建议单用户，多用户需排队
4. **Idle Reaper 阈值**：ComfyUI/Fooocus 空闲多少分钟算"空闲"？（建议 ComfyUI 10min / Fooocus 15min，可配置）
5. **驱逐默认策略**：unknown_policy 默认 warn，是否允许用户切换为 warn+ask？（✅ 2026-08-27 三层界面定稿下收敛：默认 L0 warn——兜底原则"桌面应用/前台游戏永不自动杀"；第三层设置可调）
6. **QoS 降级开关**：RED 水位自动降 0.6B 是否需要用户可关闭？（✅ 2026-08-27 三层界面定稿下收敛：QoS 降级属"有体验损失"动作 → 系统弹一次确认，用户答一次记住，可在第三层设置关闭）
7. **ComfyUI /free 的 WSL2 内存归还**：释放显存后 WSL2 内存是否及时归还宿主机？需实测验证
8. **云模型路由策略**：哪些任务自动路由云端？（P4+）
9. **隐私模式默认值**：默认全本地还是允许云端优化提示词？

---

## 十七、大赛展示要点

### 一句话介绍
**GPU Maestro-显存指挥家（GMae）**（投稿作品：GMae-16GAS）— 消费级 AI 服务器的显存编排专家：让一块 16GB（乃至 8G~48G）消费级显卡上的对话、文生图、文生视频、文生音乐等所有 AI 模型被一个编排层管理得井井有条——不打架、不死机、不手动。大厂都没解决的"Windows 消费级显存编排"空白，被一个开源项目做到极致。

### 核心亮点（30 秒电梯演讲）
1. **单卡全模态**：消费级卡同时管理 LLM 对话 + SDXL/Flux 出图 + Wan2.2 视频 + Music3 音乐（16GAS 参考实现实测）
2. **智能显存调度**：P-Eng 动态预算 + 分级释放 + 串行队列，杜绝显存打满死机
3. **秒级场景切换**：/free 模型级释放替代容器级重启，用户无感
4. **准入制门卫**：登记簿白名单 + 未登记驱逐 + 警告通知（安全）
5. **主动编排指挥家**：QoS 服务质量降级 + 空闲自动回收 + 任务队列（体验）
6. **配置驱动 + 模型登记台**：加模型/换显卡/加工作流只改配置，扫描器自动登记
7. **开源可复现**：完整 Docker Compose + 一键部署，任何消费级 N 卡机器可复刻

### 技术创新点
- **单卡显存池化**：物理显存虚拟化为可动态分配的资源池
- **错峰调度 + 主动编排**：六大场景错峰 + QoS 服务等级 + 空闲回收
- **动态预算引擎**：按模型峰值 + 底噪 + 预留实时计算，兼容 16G→24G→48G
- **准入制 GPU 治理**：进程级显存感知 + 登记簿白名单 + 分级驱逐
- **端到端进度反馈 + 预演模式**：任务前"先看谱再指挥"，调度决策显性化

### 演示建议
- **对比演示**：手动切换（停容器 30 秒）vs GPU Maestro（/free 秒级）
- **门卫演示**：开一个未登记进程 → 调度中心 3 秒内检测 → 警告/驱逐
- **QoS 演示**：跑 Flux 顶格时聊天 → 自动降级 0.6B + 通知"服务正忙"
- **预演模式演示**：点"跑 Flux"→ 界面先显示"将释放 Fooocus、对话降级、预计 4 分钟"
- **多模态编排**：同一块 16GB 卡上连续跑 出图 → 视频 → 音乐，全程自动腾挪、无手动切换、不死机（专家工作流）
- **扩展性演示**：现场用扫描器加一个新模型，UI 自动出现
- **极限挑战**：16GB 卡连续切换 6 大场景，不死机、不丢任务

### 对标与差异化
| 维度 | 传统方案 | GPU Maestro |
|------|---------|-------------|
| 多模态共存 | 需多卡或云GPU | 消费级单卡即可（8G~48G） |
| 场景切换 | 手动停容器，10-30秒 | 模型级释放，秒级 |
| 显存管理 | 用户自行判断，易死机 | 动态预算 + 自动释放 |
| 进程治理 | 无 | 准入制登记簿 + 驱逐 + 告警 |
| 用户体验 | 技术门槛高，被动 | 对话式 + 主动编排 + 预演 |
| 扩展成本 | 改代码，重新部署 | 改配置 + 扫描器登记 |

### 对标社区同类开源工具（2026-08-27 调研）
| 能力 | OneGPU4All | Model Manager | Ollama Tray Guard | gpu-orchestrator | **GMae** |
|------|-----------|---------------|-------------------|------------------|----------|
| 冲突解决（broker/排队/卸载） | ✅ | ✅ 队列 | 部分 | ✗ | ✅ |
| 多模态全栈（图/视频/音乐） | 部分（无模态认知） | ✗ 仅 LLM | ✗ 仅 Ollama | ✗ 仅诊断 | ✅ |
| 预算算账（跑前判断够不够） | ✗ | 部分 | ✗ | 软上限建议 | ✅ |
| 门卫（进程治理/驱逐） | ✗ | ✗ | 单点(Ollama) | ✗ 明确不杀 | ✅ |
| QoS 主动降级/升档 | ✗ | ✗ | ✗ | ✗ | ✅ |
| 空闲回收 | ✗ | ✗ | 部分 | ✗ | ✅ |
| 模型登记台/扫描器 | ✗ | 注册表(手动) | ✗ | ✗ | ✅ |
| Web 指挥台 | 终端仪表盘 | ✗ | 托盘 | 终端仪表盘 | ✅ Web |
| 预演模式（What-if） | ✗ | ✗ | ✗ | ✗ | ✅ |
| Windows 深度适配 | 通用 | 通用 | ✅ Windows | 通用 | ✅ Windows |

---

*文档版本：v2.8 | 创建：2026-08-26 | 更新：2026-08-27（架构演进：门卫→指挥家 + 交互设计 + 模型登记台 + 战略定位 + 命名体系 + 智算云赛道 + 竞品调研 + 市场分层与边界 + 大赛策略 + 叙事钩子 + 标语定稿"一卡，全模态/1 GPU，∞ AI/显存永不溢出" + 项目结构修正 P-Eng=vram-console）*
