# GMae 决策记录（ADR · Architecture Decision Records）

> **用途**：记录已拍板的重要决策及其理由，防止跨会话/跨 Agent 重复讨论、误推翻或走回头路。
> **规则**：只记"有取舍、易被推翻、后续要遵守"的决策；纯实现细节不入此册。新决策追加到末尾，编号不复用。状态：✅有效 / ⛔已废弃 / 🔁被取代。
> **维护**：主公拍板或 Agent 达成一致后追加；修改决策须新建 ADR 标注取代关系，不静默改写旧条目。

---

## ADR-001 GMae 独立工作区，与主仓库完全切割
- 状态：✅有效 ｜ 日期：2026-08-28 ｜ 决策者：主公
- **决定**：GMae 所有开发/规划/材料只在 `D:\Users\Danny\Documents\GMae_Amanda` 推进落盘；不回写、不读取「家庭智能中枢」主仓库（历史遗留文件仅按需参考）。
- **理由**：独立推进参赛项目，避免与主仓库相互污染。
- **影响**：任何输出路径越界即违规。

## ADR-002 三层指针记忆体系
- 状态：✅有效 ｜ 日期：2026-08 ｜ 对齐 2026 AGENTS.md 跨工具标准
- **决定**：第一层总项目（根 AGENTS.md + 总纲 v3.0 最高权威）；第二层 16GAS 子项目（16gb-ai-studio/AGENTS.md + 蓝图）；第三层模块级文档指向上层。豆包/WorkBuddy/DSH 三 Agent 共用同一套权威文件，记忆入口文件名可不同但内容指向一致。
- **否决**：每个 Agent 各记一套、互不同步。
- **影响**：文档不得与上层权威冲突；新增文档必须挂进指针链。

## ADR-003 蓝图禁止擅改
- 状态：✅有效 ｜ 日期：2026-08-28 ｜ 决策者：主公（铁律）
- **决定**：`16gb-ai-studio/docs/调度中心架构与交互设计.md` 是设计权威，任何修改（含"顺手修正/与代码对齐"）必须先给改动方案+理由、经主公批准；未批准前保持原样。
- **理由**：代码与蓝图不一致时优先怀疑代码实现，而非反向改蓝图。

## ADR-004 开源许可证采用 MIT
- 状态：✅有效 ｜ 日期：2026-09-03 ｜ 决策者：主公
- **决定**：GMae 主项目与 16GAS 子项目统一用 MIT（根 LICENSE 署名 GMae contributors，子项目署名 16GB AI Studio contributors）。
- **否决**：Apache-2.0（曾讨论，主公明确仍用 MIT）。
- **约束**：可借鉴 MIT/Apache/BSD 类代码；GPL-3.0 类（如 MMGP/deepbeepmeep、ComfyUI 本体、nvitop 的 TUI）只学思路不抄源码；新增依赖须登记 `16gb-ai-studio/THIRD_PARTY.md`。详见 `docs/开源借鉴与许可证合规指南.md`。

## ADR-005 前端纯原生 JS，零外部运行时依赖
- 状态：✅有效 ｜ 日期：2026-09-02（S4 重做时确立）
- **决定**：前端用原生 HTML/CSS/JS（SPA + hash 路由 + EventBus/State/API 自研轻量内核），不引入 React/Vue 等框架；页面 JS 按模块拆分到 `web/js/pages/`。
- **理由**：16GB 机器定位轻量、免构建、易部署；依赖越干净越利于开源与许可证合规（当前前端零第三方依赖）。
- **影响**：确需引入图形库（dagre/Cytoscape.js 等）前必须报主公批准并登记 THIRD_PARTY；React Flow 这类需 React 的不采用。

## ADR-006 后端采用轻量分层中间件，不引重型 Web 框架
- 状态：✅有效 ｜ 日期：2026-09-01（M1-M5 完成）
- **决定**：自研 `api/`（request/router/response/middleware）+ `api/endpoints/` 按域分模块；45 个端点全部迁移，旧 routes.py 仅留薄分发层。
- **理由**：零第三方依赖、可控、可测；保持与前端一致的轻量路线。

## ADR-007 唯一看门狗 engine/watchdog.py，禁止重复创建
- 状态：✅有效 ｜ 登记 WATCHDOGS.md
- **决定**：全系统唯一看门狗为 `vram-console/engine/watchdog.py`（monitoring only）；用 python.exe + 最小化窗口启动，禁用 pythonw.exe；新增/修改自启动前必须先读 WATCHDOGS.md 并停现有看门狗。
- **教训**：08-31 曾因重复看门狗堆积 194 个 server 进程（P0 事故）。

## ADR-008 文生视频统一走 ComfyUI + GGUF 量化，弃用 EasyAIVid/H3
- 状态：✅有效 ｜ 日期：2026-08-27
- **决定**：16GB 卡文生视频用 Wan2.2-TI2V-5B（经 ComfyUI + GGUF）；HunyuanVideo(H3) 因 52GB 必然 OOM 判定不可行；EasyAIVid 全量 diffusers 34GB、面向 48GB 双卡，已评估后整体删除（应用+源码+模型约 39.4GB）。
- **能力边界**：16GB 上 Wan2.2 5B 只做氛围/意境类短片（风景、慢镜头、单一主体），复杂人物动作必崩，不承诺高质量人物动态。

## ADR-009 Docker 数据迁移只用官方方法，禁用 junction/mklink
- 状态：✅有效 ｜ 日期：2026-08-26（事故教训）
- **决定**：迁移 Docker 数据用 Docker Desktop → Settings → Resources → Advanced → Disk image location；禁止用 junction/符号链接（会导致 WSL2 ext4.vhdx 丢失、镜像容器全清空）。

## ADR-010 显存安全红线：生成前必须释放到 <4GB，禁止打满
- 状态：✅有效 ｜ 依据《显存管理最高指南》
- **决定**：16GB 卡打满=死机；运行 >10GB 模型前必须先估算"模型文件+量化+中间计算≈峰值显存"，超物理上限直接标"不可行"，不硬跑。自动防死机（L1/L2/L3 分级释放）默认关闭，需用户授权才启用。

## ADR-011 系统拓扑页采用方案 A：5 层纵向树 + 容器按类折叠
- 状态：✅有效 ｜ 日期：2026-09-03 ｜ 决策者：主公确认
- **决定**：拓扑分 5 层——硬件/平台/容器/模型/任务，纵向树布局（SVG 实时生成）；容器按 core/gateway/vector_db/storage/search/monitor/other 分类，高频高权重默认展开、低频默认折叠，折叠态记 localStorage；软硬件须按宿主机实际动态探测，跨机器可移植（注册表规则驱动，不硬编码本机）。
- **否决**：横向力导向图/自由布局等其他排版方案。

## ADR-012 代码必须遵守《代码工程最高指南》，不新增技术债
- 状态：✅有效 ｜ 日期：2026-09 ｜ 决策者：主公（反复强调）
- **决定**：写代码遵循分层/状态管理/可测试性/并发安全规则；整改基于现有后端平台扩展，不写脱离平台的独立"胶水代码"；逐页 debug 时只聚焦主公当前点名的问题，不顺手扩范围。
- **配套**：跨会话防漂移机制见 `docs/跨会话多Agent工程一致性方法论.md`（任务锁、四段式交接、开发日志归档、ADR）。

## ADR-013 QoS 状态采用 ok/warning/emergency 三级
- 状态：✅有效 ｜ 日期：2026-09-02
- **决定**：QoS 实际用三级（非早期开发手册的 safe/warning/danger/critical 四级）；状态跃迁经 `_record_qos_transition` 写入事件总线。

## ADR-014 收工五步铁律
- 状态：✅有效 ｜ 日期：2026-08-31 ｜ 决策者：主公
- **决定**：声称 GMae 收工前必须完成：①更新工作日志 ②更新开发进度交接表 ③更新技术细节参考脚本 ④核查与 LLM 进化指南差异 ⑤上传 Git 和 Gitee（排除敏感与大文件）。

---

## 待登记（讨论中/未最终拍板，勿当定论）
- 是否引入 codebase-memory-mcp 作为三 Agent 共用代码图谱（P1，已调研待主公拍板）。
- 拓扑第二阶段：完整 AI 软件注册表（30+ 规则）、进程/端口/目录多维探测、未知服务手动标记 UI（方案已提，未开工）。

---

*新增 ADR 追加于上；每条含：决定 / 理由或教训 / 否决方案 / 影响。*
