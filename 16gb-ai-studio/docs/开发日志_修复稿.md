# GPU Maestro-显存指挥家 开发日志

> **项目**：GPU Maestro-显存指挥家（GMae）
> **子项目**：16G-AI-Studio（16GAS）
> **核心引擎**：Prism Engine-棱镜引擎（P-Eng）
> **记录原则**：按日期记录开发活动、问题、解决方案、决策。供后续 Agent 和维护者参考。

---

## 2026-08-25 项目启动与架构设计

### 完成工作
- 系统现状调研：OWUI、ComfyUI、Immich 已部署，模型基本就绪
- 系统架构评估：识别出显存管理混乱、服务分散、缺乏统一调度等问题
- 商业战略评估：确定路线 D（开源社区 + 技术服务）
- 撰写《显存管理最高指南》v1.3，定义 16GB 单卡调度的六大稳态和 R1-R8 红线
- 第三方独立审计报告

### 关键决策
- 显存管理是系统核心痛点，必须优先解决
- 采用场景化调度（对话/出图/视频/音乐/游戏）而非复杂的资源分配算法
- 切换场景必须"先释放后占用再验证"，杜绝显存打满死机

### 经验教训
- 16GB 单卡物理上限是硬约束，所有设计必须围绕这个约束展开
- 底噪 1.0-1.1GB 不可忽视，实际可用显存约 15GB

---

## 2026-08-26 上午 ollama 容器化（波折重重）

### 背景
ollama 原本装在 Windows 宿主机，为了统一 Docker 管理和便于迁移，决定容器化。

### 遇到的问题与解决方案

**问题 1：Docker Hub 拉取极慢**
- 直连 Docker Hub 拉 ollama/ollama:latest（7.2GB），速度几十 KB/s，频繁断开
- 尝试阿里云镜像：`registry.cn-hangzhou.aliyuncs.com/ollama/ollama`，该仓库不存在
- 尝试 exit node（东京~~新加坡~~）：速度有提升但仍不稳定（新加坡已于 2026-08-26 下线）

**问题 2：aria2 多线程下载大文件内容损坏**
- 在东京 VPS 用 docker pull → docker save → tar → aria2 多线程下载
- aria2 `-x16 -s16` 下载的文件大小正确但缺少 manifest.json，docker load 失败
- **结论**：aria2 多线程单文件下载大文件不可靠，改用分割文件法

**问题 3：最终方案——分割文件 + 批量下载**
- 东京 VPS：docker pull → docker save → `split -b 100M` 分割成 33 个
- 本机：aria2 批量下载（每文件单线程 `-x1 -s1`，并发 `-j4`）
- 不完整文件用 PowerShell HttpWebRequest 带 Range 断点续传循环重试
- `copy /b` 合并 → `tar -tf` 验证 manifest.json → docker load 成功

**问题 4：新加坡 VPS 配置**（⚠️ 该 VPS 已于 2026-08-26 下线，以下为历史记录）
- ~~新部署新加坡 VPS（149.28.134.107，Ubuntu 24.04）~~
- ~~接入 Tailscale（100.103.131.14，ai-exit-sg）~~
- ~~配置为 exit node，延迟 ~71ms（P2P），比东京快 3-4 倍~~

### 完成工作
- ollama 容器化成功，7 个模型全部继承，GPU 加速正常
- 容器配置：`docker run -d --name ollama --restart unless-stopped -p 11434:11434 --gpus all -v D:\ollama\models:/root/.ollama/models ollama/ollama:latest`
- 镜像备份：`_installers/ollama.tar`（7.2GB），新机器部署可直接 docker load
- 验证：qwen3.5:9b 推理 44 token/s（ComfyUI 停止后）

### 关键决策
- 大文件下载采用 VPS 中转 + 分割文件 + 单线程批量下载方案，放弃 aria2 多线程
- ~~新增 exit node 作为主要下载通道，东京作为备用~~（新加坡已于 2026-08-26 下线，当前唯一 exit node 为东京）

### 经验教训
- aria2 多线程下载大文件（>1GB）可能内容损坏，大小正确不代表内容正确
- Docker 镜像备份成 tar 是新机器部署的最快方式，避免重复拉取
- ~~exit node 节点选择很重要，新加坡比东京快 3-4 倍~~（新加坡已于 2026-08-26 下线）

---

## 2026-08-26 下午 服务访问排查

### 问题
用户反馈其他 Tailscale 设备（手机/笔记本）无法访问 OWUI 和调度中心。

### 排查过程
1. 检查 Docker 服务监听：全部 0.0.0.0，正常
2. 检查 Tailscale 接口 NetworkCategory：Private，正常
3. 检查防火墙规则：Docker Desktop Backend 允许 Private，正常
4. 本机通过 Tailscale IP 访问 OWUI：HTTP 200，正常
5. **最终发现**：调度中心进程多次自动停止（pythonw 不稳定），重启后恢复

### 解决方案
- 调度中心监听地址从 127.0.0.1 改为 0.0.0.0
- 创建开机自启项 `Startup\VRAM_Console.vbs`
- 后续改用 python.exe + 日志方式运行（见下）

### 经验教训
- pythonw 无窗口模式出错时看不到错误信息，不适合长期运行的服务
- 服务监听 0.0.0.0 是内部 Tailscale 访问的前提

---

## 2026-08-26 下午 推理速度澄清

### 问题
用户质疑推理速度只有 13.5 token/s，是否容器化导致性能损耗。

### 分析
- 13.5 token/s 是 qwen3:0.6b 在 ComfyUI 占用 7.8GB 显存时的速度
- 切换到 game 场景（停 ComfyUI）后，qwen3.5:9b 跑到 43.97 token/s
- **结论**：容器化无性能损耗，速度差异源于显存调度（ComfyUI 占用显存导致 LLM 可用显存减少）

### 经验教训
- 显存共享是单卡多模态的核心矛盾，调度策略直接影响各服务性能
- 评估推理速度必须说明当前显存占用情况，否则数据无意义

---

## 2026-08-26 傍晚 调度中心 v2 修复（对齐最高指南）

### 发现的问题（对照《显存管理最高指南》）
1. README 写 6 场景，代码只有 5 个（缺 H3 出视频场景）
2. BIG_MODELS 硬编码只有 9b/0.6b，27B 模型停不掉
3. ollama 容器化后，宿主机 CLI 调用失效
4. 切换场景前无显存预检（违反 M1 铁律）

### 修复内容
1. **新增 h3 场景**：启动 comfyui、停 fooocus、释放显存、等 8188 就绪
2. **BIG_MODELS 扩展**：包含两个 27B 模型；ollama_stop_all 改为用 `/api/ps` 动态获取已加载模型
3. **docker exec 适配**：所有 ollama stop/run 改为 `docker exec ollama ollama ...`
4. **显存预检**：scene_switch 开头检查 free_mb < 4096 时自动执行 gpu_release.ps1
5. **默认 HOST**：从 127.0.0.1 改为 0.0.0.0
6. **LAST_SCENE 逻辑**：支持 h3 场景状态推断

### 验证
- 语法检查通过
- combo none（docker exec 停止所有模型）：成功
- H3 场景切换：成功（启动 comfyui + 停 fooocus + 释放显存 + 等待就绪 10s）
- 切回对话态：成功

### 踩坑
- 旧 python.exe 进程（PID 57256）一直在运行旧代码，导致测试返回 "unknown scene: h3"
- 杀掉所有 python* 进程、清 __pycache__、重启后才生效
- **教训**：修改代码后必须确认旧进程完全退出，否则测试的是旧代码

---

## 2026-08-26 傍晚 进程稳定性问题

### 问题
调度中心反复挂掉，pythonw 进程自动退出，无错误信息。

### 临时解决方案
- 改用 python.exe + 重定向日志到 `%TEMP%\vram_console.log`
- 创建 `start_with_log.bat` 启动脚本
- 更新开机自启项 VRAM_Console.vbs 使用带日志方式

### 待解决
- 崩溃根本原因待排查（需要等待下次崩溃时查看日志）
- P0 看门狗自动重启

### 经验教训
- 长期运行的服务必须有日志，否则出问题无法排查
- pythonw 适合 GUI 程序，不适合无窗口的后台服务

---

## 2026-08-26 晚上 架构设计与命名

### 完成工作
- 讨论全链路 AI 服务管理流程和显存管理策略
- 撰写《调度中心架构蓝图》v1.0（14 章设计框架）
- 确定 5 条核心设计原则：可靠性优先、效率最大化、用户友好、可扩展、可维护
- 设计 P0-P5 实施路线图

### 命名确定
- **项目总名**：GPU Maestro-显存指挥家（GMae）
- **核心引擎**：Prism Engine-棱镜引擎（P-Eng）——一块 GPU 如棱镜般分出多种模态
- **首个子项目**：16G-AI-Studio（16GAS）
- **标语**：One GPU, Infinite Models

### 大赛定位
- 赛事：2026 上海开源软件应用创新大赛
- 赛道：智算云（异构算力调度与 GPU 池化 / 推理加速）
- 核心卖点：16GB 消费级卡跑全模态 AI，智能显存调度

### 关键决策
- ComfyUI 内部模型无法精确监控，但可通过工作流静态分析 + nvidia-smi 总量推断
- 优先模型级释放（/free，秒级），不轻易停容器（10-30秒）
- 配置驱动架构，所有可变资源放 registry.json，代码只处理逻辑

---

## 2026-08-26 晚上 P0 稳定性实施（日志 + 看门狗 + 健康检查）

### 背景
调度中心进程反复崩溃，pythonw 无窗口模式出错看不到原因，服务不可用。

### 完成工作

**P0-1 结构化日志系统**
- server.py 添加 logging 模块，TimedRotatingFileHandler 按天轮转，保留 30 天
- 日志格式：`时间 | 级别 | JSON事件`，JSON 包含 ts/event/具体字段
- 关键事件日志：server_start、server_crash、scene_switch_start/done、vram_pre_release、security_warning
- 日志文件：`vram-console/logs/vram-console.log`

**P0-2 看门狗自动重启**
- 创建 watchdog.py，用 subprocess.Popen 监控 server.py
- server 退出后 5 秒自动重启，1 小时内最多重启 10 次（防死循环）
- 看门狗日志：`logs/watchdog.log`，记录启动/退出/重启事件
- 验证：杀掉 server 进程后，5 秒自动重启成功

**P0-3 健康检查 API**
- 新增 `GET /api/health` 端点（无需认证，方便外部监控）
- 检查项：GPU 状态、Ollama 连通性（11434）、ComfyUI 连通性（8188）、显存阈值警告
- 返回整体 ok 状态和各服务详情

### 踩坑记录

**坑 1：pythonw 启动 watchdog 不稳定**
- 用 pythonw.exe 启动 watchdog.py，进程会莫名消失
- 改用 python.exe 启动正常，VBS 隐藏窗口
- **教训**：pythonw 适合 GUI 程序，不适合需要 subprocess 的后台服务

**坑 2：bat 循环看门狗不可靠**
- 最初用 watchdog.bat 循环启动 python，cmd 进程会莫名退出
- 改用 Python watchdog.py + Popen 稳定可靠

**坑 3：跨 Bash 调用进程树被清理**
- 在一个 Bash 命令启动 watchdog，另一个 Bash 命令杀 server，watchdog 也消失
- 在同一个 Bash 命令里完成完整测试，自动重启正常
- **教训**：测试后台进程时，启动和验证尽量在同一会话完成

**坑 4：subprocess.run vs Popen**
- 最初用 subprocess.run 等待子进程，可能因输出管道阻塞导致问题
- 改用 Popen + poll() 循环，stdout/stderr 重定向到 DEVNULL，稳定可靠

### 当前状态
- 服务通过 watchdog.py 托管，崩溃自动重启
- 所有操作有结构化日志，出问题可追溯
- /api/health 可用于外部监控

---

## 2026-08-26 晚上 看门狗统一管理

### 背景
用户担心在 agent 环境下看门狗失控，要求盘查所有看门狗并统一登记管理。

### 盘查结果
- 发现 5 个启动脚本混杂：watchdog.bat（旧）、watchdog.py（新）、watchdog.vbs、start.bat（旧）、start_with_log.bat
- 开机自启项 VRAM_Console.vbs 错误指向旧的 watchdog.bat
- 当前无服务运行

### 完成工作
1. **清理废弃脚本**：删除 watchdog.bat、start.bat（旧）、start_with_log.bat
2. **保留唯一看门狗**：watchdog.py（Popen + poll 循环，稳定可靠）
3. **创建启动脚本**：run_watchdog.bat（自动查找 python.exe 路径，解决 PATH 问题）
4. **统一管理脚本**：start.bat（启动）、stop.bat（停止）、status.bat（状态查看）
5. **创建登记文件**：WATCHDOGS.md（所有看门狗/自启动/定时任务的统一登记册）
6. **修复开机自启**：Startup\VRAM_Console.vbs 指向正确的 run_watchdog.bat

### 踩坑记录

**坑 1：pythonw 启动 watchdog 不稳定**
- pythonw.exe 启动 watchdog.py，subprocess 子进程会莫名消失
- 改用 python.exe + 最小化窗口稳定

**坑 2：VBS 引号转义问题**
- VBS 用 `WshShell.Run "cmd /c ""path""", 0` 引号嵌套容易出错
- 简化为设置 CurrentDirectory 后直接 `WshShell.Run "run_watchdog.bat", 0`

**坑 3：沙箱环境后台进程限制**
- AI Agent 通过 Start-Process 启动的后台进程，在工具调用结束后被沙箱清理
- 解决方案：用户手动双击 start.bat，或依赖开机自启项
- Agent 不应依赖工具调用的持久化后台进程

**坑 4：python.exe 不在系统 PATH**
- VBS 直接调用 python.exe 可能找不到
- run_watchdog.bat 用 `where python.exe` 自动查找路径

### 当前状态
- 看门狗统一管理完成，唯一看门狗 watchdog.py
- 所有脚本已清理统一，WATCHDOGS.md 登记完整
- 服务需用户手动启动（双击 start.bat）或重启电脑（开机自启生效）

---

## 2026-08-26 晚上 P1 资源注册表实施（配置驱动）

### 背景
模型名硬编码在 server.py 的 BIG_MODELS 和 combo_switch 中，加模型/改参数需要改代码。

### 完成工作
1. **创建 resources/registry.json**，统一资源注册表，包含：
   - ollama 模型列表（4 个 LLM 模型，含 vram_gb/ctx/exclusive/category/combo）
   - ollama combos 配置（9b/0.6b/27b-rvn/27b-iq3xxs/none）
   - comfyui 模型列表（SDXL/Flux/H3/Music3）
   - containers 配置
   - scenes 场景定义（6 个场景）
   - system 系统参数（显存总量/底噪/预留/M1阈值/看门狗参数）
2. **修改 server.py**：
   - 添加 load_registry() 函数，启动时加载配置
   - BIG_MODELS 从 registry 动态生成（过滤 category=llm 的模型）
   - OLLAMA_CONTAINER 从 registry 读取
   - combo_switch 完全重写，从 registry 读取 combos 配置和模型元数据
   - 失败时用硬编码兜底，保证兼容性
3. **测试验证**：
   - registry_loaded 日志确认加载成功（models_count=4）
   - combo none 从 registry 读取 4 个模型全部停止成功
   - 语法检查通过

### 关键设计
- **配置驱动**：加模型只改 registry.json，不改代码
- **向后兼容**：registry 加载失败时用硬编码兜底
- **元数据丰富**：每个模型标注 vram_gb/ctx/exclusive，为未来显存预算引擎提供数据
- **CPU 模型过滤**：BIG_MODELS 只包含 LLM 模型，embedding/reranker（CPU）不参与显存管理

### 待后续
- gpu_release.ps1 也需要改为读取 registry.json（当前仍硬编码）
- 前端 UI 模型列表从 registry 动态生成
- 场景定义从 registry 读取（当前 scene_switch 仍硬编码）

---

## 2026-08-26 晚上 启动脚本修复（WindowsApps python 存根）

### 问题
重启系统后调度中心无法自动启动，手动双击 start.bat 也失败。

### 根因
`where python.exe` 返回的第一个结果是 `C:\Users\Danny\AppData\Local\Microsoft\WindowsApps\python.exe`，这是**微软商店的安装提示存根**，不是真正的 python。运行时会提示 "Python was not found"，但因为是最小化窗口启动，错误被隐藏。

真正的 python 在豆包沙箱路径：`C:\Users\Danny\AppData\Local\Doubao\User Data\sandbox_runtime\bases\9f6d27f23933fb44a3a1c728c88a5ce4\python\python.exe`

### 修复
修改 `start.bat` 和 `run_watchdog.bat`：
1. 遍历 `where python.exe` 所有结果，用 `findstr /i "WindowsApps"` 跳过存根
2. 找不到时回退到豆包沙箱路径（自动扫描 `bases\*` 目录）
3. 开机自启 VBS 指向 run_watchdog.bat，用真正的 python，链路打通

### 教训
- Windows 的 `where python` 不可靠，必须排除 WindowsApps
- 最小化窗口启动会隐藏错误，调试时先用普通窗口
- 重启后验证自启是否真正生效（不能只看 VBS 存在）

---

## 2026-08-26 晚上 health 检查修复（import 缺失 + 容器停止）

### 问题
调度中心页面持续显示 "红色 Ollama 无响应（假死特征）"，health 检查 ollama/comfyui 均为 false。

### 根因（两个独立问题）
1. **server.py 顶部缺少 `import urllib.request`**：health_check() 中使用 `urllib.request.Request` 触发 NameError，被 `except Exception` 静默捕获，永远返回 false。而 load_model_api() 等函数在局部有 `import urllib.request`，所以那些功能正常，只有 health_check 受影响。
2. **ComfyUI 容器已停止**：状态 Exited (143)，被 SIGTERM 杀掉，重启系统后未自动启动。

### 修复
1. server.py 顶部添加 `import urllib.request`
2. `docker start comfyui` 启动容器
3. 前端假死警告提示改为容器化版（`docker restart ollama`）

### 教训
- `except Exception` 会吞掉 NameError 等编程错误，健康检查类函数应单独测试
- 重启系统后必须检查所有容器是否自动启动（`docker ps -a`）
- Python 标准库导入必须在顶部统一管理，避免局部导入导致部分函数可用部分不可用

---

## 2026-08-26 晚上 重大架构问题：宿主机 Ollama 与 Docker Ollama 并存

### 问题现象
调度中心显示显存 15.4G 超警戒，当前场景 ComfyUI，但 Ollama 的 qwen3.5:9b（8.9G）和 qwen3:0.6b（1.0G）还在显存中。切换场景时 gpu_release.ps1 调用 `ollama stop` 无效。

### 根因（重大发现）
**宿主机安装了 Ollama Windows 版，且开机自启（`D:\scripts\start_ollama_serve.cmd`），与 Docker 容器里的 Ollama 并存。**

- 宿主机 Ollama 抢占 11434 端口，Docker Ollama 的端口映射失效
- 调度中心访问 `127.0.0.1:11434` 实际访问的是宿主机 Ollama
- `docker exec ollama ollama stop` 停止的是 Docker 容器里的 Ollama（空模型），所以显存不释放
- gpu_release.ps1 用宿主机 `ollama stop` 命令，但 Ollama 容器化后宿主机 CLI 可能已失效
- 两个 llama-server.exe 进程（PID 3060, 5984）占用了约 8G 显存

### 修复
1. **停止宿主机 Ollama 进程**：`Stop-Process ollama*, llama-server*`
2. **禁用宿主机 Ollama 开机自启**：移除注册表 `HKCU\...\Run\OllamaServe`
3. **重启 Docker Ollama**：重新绑定 11434 端口
4. **修复 gpu_release.ps1**：改为用 `docker exec ollama ollama stop`，从 registry.json 读取模型列表
5. **修复 scene_switch**：在 comfy/h3/music/fooocus 场景开头显式调用 `ollama_stop_all()`

### 验证
- 显存从 13.7G 降到 5.9G（释放约 8G）
- Docker Ollama 7个模型全部正常，端口 0.0.0.0:11434 监听
- 宿主机 Ollama 开机自启已禁用，重启后不会再冲突

### 教训
- **容器化后必须彻底禁用宿主机版本**，包括进程、服务、开机自启
- `docker exec` 只影响容器内，不影响宿主机进程
- 端口冲突时 Docker 端口映射会静默失败，docker ps 显示 `11434/tcp` 而非 `0.0.0.0:11434->11434/tcp`
- 显存异常时先查 `nvidia-smi --query-compute-apps` 查实际占用进程，不要假设是容器内的

---

## 2026-08-26 深夜 ComfyUI 模型状态显示 + 模型台帐 + 大赛素材

### 完成工作

**1. ComfyUI 模型状态显示（方案 A）**
- ComfyUI 无公开的"当前加载模型"API，采用方案 A：从 /history 取最近成功工作流，解析模型加载节点（CheckpointLoaderSimple/UNETLoader），匹配 registry.json 元数据
- 添加 likely_loaded 字段，结合实际显存验证（实际占用 > 推断显存 40% 且 > 2G 才认为还在显存）
- BUDGET_ROWS 添加 model_id，每个 ComfyUI 模型（SDXL/Flux/Music3/H3）独立显示状态

**2. 模型实测台帐系统**
- 创建 `docs/模型实测台帐.md`（人类可读汇总表+校准结论）
- 创建 `docs/model_benchmarks.jsonl`（机器可读原始数据）
- 记录每次生成的参数、显存峰值、时长、效果评价

**3. 显存校准**
- Music3 从 14.5G 改为 **13G**（实测）
- Flux-Q5 从 13G 改为 **11.7G**（实测，GGUF Q5 量化）
- MiniMax-H3 从 14G 改为 **14.7G**（实测，用 512×512/25帧）
- SDXL 6.5G（保持）

**4. Flux GGUF 加载修复**
- 根因：ComfyUI-GGUF 节点装到了 `/workspace/custom_nodes/`，但实际加载目录是 `/opt/ComfyUI/custom_nodes/`
- 二次根因：gguf pip 包装到了系统 Python，ComfyUI 用的是 `/opt/environments/python/comfyui/` 虚拟环境
- 修复：复制节点到正确目录 + 用 ComfyUI venv 的 pip 安装 gguf
- 验证：UnetLoaderGGUF/CLIPLoaderGGUF 等 6 个节点加载成功，Flux 生成成功（11.7G，~150秒）

**5. H3 comfy_aimdo 修复**
- 问题：640×640/49帧时 SamplerCustomAdvanced 报 `HostBuffer.read_file_slice failed`
- 尝试禁用 comfy_aimdo → ComfyUI 启动失败（镜像深度集成，不可禁用）
- 解决方案：降低参数至 512×512/25帧，生成成功（14.7G，262秒）
- 更新工作流默认参数，registry.json 添加 max_resolution/max_frames 限制

**6. 大赛素材**
- SDXL 文生图：成功（赛博朋克 AI 控制中心，31秒）
- Flux 文生图：成功（11.7G，~150秒）
- Music3 文生音乐：成功（30秒电子合成器，42秒）
- H3 文生视频：成功（512×512/25帧，14.7G，262秒）
- 4 种模态素材全部就绪

### 踩坑记录

**坑 1：ComfyUI 自定义节点目录不是 /workspace/custom_nodes/**
- ai-dock 镜像里 ComfyUI 装在 `/opt/ComfyUI/`，自定义节点目录是 `/opt/ComfyUI/custom_nodes/`
- `/workspace/custom_nodes/` 可能是符号链接或不存在，装进去不会被加载
- **教训**：装自定义节点前用 `docker logs comfyui | grep custom_nodes` 确认实际加载路径

**坑 2：pip install 装到错误的 Python 环境**
- ComfyUI 用独立 venv：`/opt/environments/python/comfyui/`
- 系统 pip 安装的包 ComfyUI 看不到
- **教训**：容器内的 pip 包必须用 `/opt/environments/python/comfyui/bin/pip install`

**坑 3：comfy_aimdo 不可禁用**
- ai-dock 镜像深度集成 comfy_aimdo，重命名目录后 ComfyUI 启动报 ModuleNotFoundError
- **教训**：第三方插件问题优先调参数，不要轻易禁用镜像预装组件

**坑 4：Music3 工作流缺参数**
- MiniMaxMusic3TextEncode 节点需要 caption/lyrics/seed/cfg_scale/top_k 参数
- 原工作流只有 text 字段，提交失败
- **教训**：从 ComfyUI 导出的工作流要验证所有必填字段

---

## 2026-08-27 凌晨 Docker 磁盘空间清理与迁移（紧急）

### 问题
C 盘仅剩 7GB，系统告警。

### 根因分析
1. **Docker 镜像 211GB**：comfyui-h3 的 v1-v4 四个版本，每个 ~48GB（共 193GB）；多个重复镜像（OWUI 3个、fooocus 2个、ComfyUI 2个）；未使用的 dify 相关镜像
2. **WSL2 vhdx 不收缩**：删除镜像后，`docker_data.vhdx` 仍为 226GB，diskpart compact 无效
3. **本地 Ollama 模型 1.46GB**：已容器化但本地模型未删除

### 清理步骤
1. 删除 comfyui-h3:v1/v2/v3（保留 v4）
2. 删除重复镜像（旧 registry 的 OWUI/fooocus/ComfyUI）
3. 删除未使用的 dify/nginx/postgres/qdrant 镜像
4. docker container prune + image prune
5. Docker 镜像从 211GB 降到 41GB

### 迁移到 D 盘（符号链接方案）
- WSL 导出发行版只有 60MB（数据不在发行版，在 docker_data.vhdx）
- diskpart compact vhdx 无效（WSL2 格式特殊）
- **最终方案**：停止 Docker → wsl --shutdown → 移动 docker_data.vhdx 到 D:\DockerData\wsl\disk\ → 在原位置创建 junction 符号链接 → 再启动 Docker
- Docker 无感知，所有容器自动启动，服务正常

### 结果
- C 盘：7GB → **244.5GB**（回收 237GB）
- D 盘：减少 226GB
- Docker 数据位置：`D:\DockerData\wsl\disk\docker_data.vhdx`（通过 junction 链接）
- 所有 9 个容器自动启动，ComfyUI/Ollama/OWUI 正常

### 踩坑记录

**坑 1：WSL2 vhdx 删除文件后不自动收缩**
- docker system df 显示 41GB，但 vhdx 文件仍是 226GB
- diskpart compact vdisk 对 WSL2 的 vhdx 无效
- **教训**：WSL2 虚拟磁盘需要特殊处理，常规压缩工具无效

**坑 2：docker commit 产生大量镜像**
- 每次 docker commit comfyui 都创建一个新镜像（~48GB），v1-v4 共 193GB
- 镜像分层共享基础层，但每次 commit 的可写层差异仍占大量空间
- **教训**：docker commit 后及时删除旧版本，或用 Dockerfile 重建镜像

**坑 3：Docker Desktop 数据不在 WSL 发行版中**
- `wsl --export docker-desktop` 只有 60MB
- 数据在 Docker Desktop 管理的 `docker_data.vhdx` 中，不是标准 WSL 数据
- **教训**：迁移 Docker 数据要操作 vhdx 文件，不是 WSL 发行版

**坑 4：符号链接（junction）对 Docker 透明**
- `mklink /J` 创建的目录联接对 WSL2 和 Docker 完全透明
- Docker 启动时通过 junction 访问 D 盘的 vhdx，无需修改配置
- **经验**：junction 是迁移 Windows 应用数据的简单有效方式

### 长期建议
- 定期 `docker system prune` 清理悬空镜像
- docker commit 后立即删除旧版本
- 大文件/数据目录优先放 D 盘，用 junction 链接
- 监控 C 盘空间，低于 50GB 时告警

---

## 待记录（后续补充）

- [ ] P0 日志系统 + 看门狗实施记录
- [ ] 进程崩溃根本原因排查结果
- [ ] P1 registry.json 实施记录
- [ ] OWUI 与 ComfyUI 打通记录
- [ ] 开源大赛报名材料准备记录

---

*文档版本：v1.0 | 创建：2026-08-26 | 每次开发活动后追加记录*

---

## 2026-08-26 深夜 Docker 数据丢失事故（最严重）

### 事故经过
1. C 盘空间紧张，排查发现 Docker WSL2 虚拟磁盘（ext4.vhdx）占用大量空间
2. 未使用 Docker 官方迁移工具，而是尝试用 junction 符号链接迁移
3. 操作过程中 ext4.vhdx 文件丢失，所有 Docker 镜像、容器、卷数据清空
4. 9 个镜像全部丢失：ollama、redis、searxng、immich-server、immich-machine-learning、immich-postgres、open-webui、caddy、ai-dock/comfyui

### 恢复过程
1. 从日本 VPS Registry 重新拉取镜像（部分成功）
2. ComfyUI 镜像从日本 VPS 分割下载合并恢复
3. ollama 模型从宿主机 D:\ollama\models 挂载恢复（7个模型）
4. 所有容器重新创建，配置逐步恢复
5. 耗时：约 6 小时

### 根本原因
- 对 Docker Desktop 数据存储机制理解不足
- 急于解决空间问题，没有先研究官方方案
- junction 符号链接对 WSL2 虚拟磁盘不适用

### 教训
- **Docker 数据迁移必须用官方方法**：Docker Desktop → Settings → Resources → Advanced → Disk image location
- 禁止用 junction/mklink 迁移 Docker 数据
- 关键基础设施操作前必须备份
- 镜像备份成 tar 是最快的恢复方式

---

## 2026-08-26 深夜 ComfyUI 升级与 PyTorch 修复

### 背景
事故后恢复的 ComfyUI 是旧版本（v0.2.2, PyTorch 2.4.1），H3/Music3 节点无法加载。

### 遇到的问题

**问题1：PyTorch wheel 文件损坏（犯了两次）**
- 从日本 VPS 下载 torch-2.5.1+cu121.whl（744MB）
- aria2 默认预分配磁盘空间，文件显示 744MB 但实际没下完
- pip install 报 "Wheel is invalid"
- **错误**：根据文件大小判断下载完成，没有验证完整性
- **解决**：vps-download-relay Skill 添加强制验证（verify_file.py 检查 ZIP 完整性）

**问题2：comfy_kitchen 类型注解不兼容**
- PyTorch 2.5.1 的 infer_schema 不支持 list[int]
- 报错：Parameter kernel_size has unsupported type list[int]
- **解决**：手动 patch comfy_kitchen 源码，list[int] → typing.List[int]，添加 import typing

**问题3：Music3 第一次测试 OOM**
- fooocus 同时在生成图片，显存打满导致 ComfyUI 容器崩溃
- **错误**：提交大模型任务前没有检查其他服务的显存占用
- **解决**：重启容器，单独运行 Music3

### 完成工作
- ComfyUI 升级到 v0.34.0
- PyTorch 升级到 2.5.1+cu121
- comfy_kitchen 类型注解补丁
- H3 节点 15 个 + Music3 节点 4个 全部加载
- 容器 commit 为 ai-dock/comfyui:gmae-v0.34（21.4GB）

### 教训
- 文件大小≠下载完成，必须验证完整性
- 大模型运行前必须 nvidia-smi 检查空闲显存
- 升级依赖后可能需要 patch 第三方库

---

## 2026-08-26 深夜 H3 模型 OOM 崩溃

### 事故经过
1. 看到台帐记录"H3 跑通（512×512, 25帧, ~14.7G）"
2. 没有重新评估模型大小，直接提交 H3 文生视频工作流
3. H3 模型总计 52GB（diffusion 31.7G int8 + text encoder 14.6G + VAE 5.4G）
4. 16GB 卡无法承载，显存打满，Docker 守护进程不稳定
5. 调度中心停摆，系统需要重启 Docker Desktop 恢复

### 根本原因
- 盲目信任历史台帐记录，没有做"模型大小 vs 显存"的基本评估
- 旧台帐记录的"跑通"可能不准确或用了极端参数

### 处理
- 删除所有 H3 模型文件（释放 52.4GB）
- 删除 Ollama 27B 模型（qwen3.8:27b-rvn-q3km 13G + qwen3.8:27b-iq3xxs 10G，释放 23GB）
- 总计释放约 75GB 磁盘空间
- 台帐更新：H3 标记为"16GB卡不可行，需24GB+"

### 教训
- 任何 >10GB 模型运行前，必须计算：模型大小 + 量化方式 + 中间计算 ≈ 峰值显存
- 超过物理上限的模型直接标记为"不可行"，不要尝试
- 历史记录必须验证，不能盲目信任

---

## 2026-08-26 深夜 Music3 测试结果

### 测试参数
- 模型：MiniMax Music3（fp8 加载）
- 参数：5秒音频，10步，cfg=1.5，seed=42
- 提示词：lo-fi hip hop beat with warm piano chords
- 峰值显存：~9.8GB
- 生成时长：147.85秒

### 结果
- 技术上跑通了，输出 FLAC 文件
- 效果评价：⭐ 不及格（只有调子，无音乐感）
- 可能原因：fp8 量化 + 10步太少 + 5秒太短

### 教训
- 跑通≠好用，质量评估必须跟上
- Music3 可能需要更多步数和更长时长才能出好效果
- 16GB 卡限制了参数，质量可能无法达标

---

## 2026-08-26 深夜 Ollama 模型测试发现

### 问题
- 用 /api/generate（completion）测试 Qwen3.5:9b，中文乱码 + CoT 思维链泄露
- Qwen3 系列有内置 thinking 能力，completion 模式不会正确应用 chat template

### 解决
- 测试 LLM 必须用 /api/chat 端点
- Qwen3.5:9b（原版）：Q4_K_M，有 vision，中文 system prompt
- realigned-qwen3.5:9b：Q4_K_S，无 vision，context 更小（8192）
- 两个模型推理速度差不多：~44 token/s

### 教训
- 有 thinking/vision 等特殊能力的模型，必须用 chat 端点
- 测试 LLM 时要注意模型的量化方式和特殊能力

---

## 今日总结

### 完成
- ✅ Docker 服务全部恢复（10个容器）
- ✅ ComfyUI 升级到 v0.34.0 + PyTorch 2.5.1
- ✅ H3/Music3 节点全部加载
- ✅ Music3 文生音乐跑通
- ✅ 大模型清理（H3 + 27B），释放 75GB
- ✅ vps-download-relay Skill 创建

### 未完成
- ❌ H3 文生视频（16GB卡不可行）
- ❌ ComfyUI 容器用新镜像 gmae-v0.34 重建
- ❌ 旧镜像 gmae-restored 清理

### 最深的教训
1. **基础设施 > 功能开发**：今天 80% 时间花在恢复环境上
2. **验证 > 假设**：文件下载完要验证、模型能跑要验证、服务恢复要验证
3. **评估 > 尝试**：大模型运行前必须算显存账，不能盲目跑

---

## 2026-08-27 Wan2.2-TI2V-5B 文生视频（I2V）实战

### 背景
- H3 52GB OOM 不可行后，换 Wan2.2-TI2V-5B（9.3GB FP16）
- 关键坑：5B TI2V 必须用 wan2.2_vae.safetensors（48通道），不能用 wan_2.1_vae（16通道）

### 产出
| 素材 | 结果 | 评分 | 状态 |
|------|------|------|------|
| 海边白裙（v2） | 720x480→超分1440x960 | 60分→清晰度提升 | ✅ 定稿 |
| 赛博朋克上海（第1稿） | 文字不可读/车辆乱/光影闪 | ❌ 失败 | 弃 |
| 机器人（v3） | 720x480, 65帧@16fps=4s | 70分（动作偏弱） | ✅ 先这样 |

### 关键经验（I2V 提示词）
1. **禁止 flicker 字眼**：提示词写 "billboards flicker" 会诱导灯光不稳定闪烁。改 "steady glow / stable lighting"
2. **动作要明确动词+幅度**：写 "slowly raises its head upward" 而不是 "gently tilts / subtle motion / extremely slowly"——Wan2.2 对弱化词会直接不动
3. **脉动要写周期**："pulses bright and dim in a breathing cycle"，"soft pulsing" 太弱不生效
4. **首帧越"干净"越稳**：无车辆、无文字招牌、无正脸（远景背影可）→ 避免车辆乱动/文字乱码/脸部漂移
5. **负面要带**：gibberish text, unreadable signage, flickering lights, morphing, deformed limbs
6. **帧数显存账**：65帧@720x480 峰值~10.9GB；120帧 attention≈65帧的3.4倍必OOM。24fps下65帧=2.7s，16fps=4s

### 显存冲突事故（重要）
- 调度中心切到 fooocus 场景后 Fooocus 驻留~4GB 显存；同时直接调 ComfyUI API 跑 I2V（11GB），叠加后 15GB/16.4GB，只剩 1GB 接近死机
- 教训：**直接用 ComfyUI API 跑任务前，必须确认调度中心当前场景不驻留其他 GPU 服务**。必要时先 docker stop 冲突容器
- 处理：docker stop fooocus 释放显存，保住 I2V 任务

### 交付规格
- 视频超分链路：ComfyUI LoadVideo→RealESRGAN_x4plus→bicubic 1440x960→CreateVideo 16fps
- 输出目录：/output/（容器内），同步到项目 outputs/ 目录
