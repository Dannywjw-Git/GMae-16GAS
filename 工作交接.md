# 工作交接文档

> **用途**：跨 Agent（豆包/WorkBuddy/DSH）、跨会话的工作衔接。每个 Agent 结束工作前必须更新此文件。
> **最后更新**：2026-08-31（高负载三层应对策略全部落地；调度中心已恢复运行；/api/status 冷路径性能优化 2007ms→681ms，98 测试全过；Skill 资产检查完成——model-benchmark-suite 未落盘为正式 Skill 但有 docs/models/ 方法论体系，model-benchmark-suite 已封装完成（已验证脚本实测可用））
> **更新者**：豆包（继续推进会话）

---

## 🚀 新会话快速上手（5 分钟）

### 1. 当前在做什么？
**前端 v2.0 模块化重写**——旧版 index.html（单文件 2214 行、69 全局函数、291 全局变量）问题太多，用户测试发现几乎每个功能都有 bug，决定从零开始用 ES Modules 模块化重写。

### 2. 现在进行到哪一步？
- ✅ **版本隔离已完成**：旧版归档到 `legacy/v1-index.html`，server.py 支持 `FRONTEND_VERSION` 环境变量切换（v2 默认 / v1 回滚）
- ✅ **基础框架已完成**：CSS 变量/重置/主样式、入口 HTML/JS、静态文件服务
- ✅ **核心模块已完成（2026-08-30 晚间）**：`core/utils.js`、`core/events.js`、`core/api.js`（封装全部后端 API）、`core/state.js`、`core/router.js`（哈希路由）
- ✅ **阶段1 总览页（dashboard）已建**：L0 显存水位能量条 + L1 动作（一键释放/预演占位）+ 统计卡 + 服务活跃度，10s 轮询
- ✅ **组件 CSS 基础版**：button/card/sidebar/modal/toast/table/form/chart 骨架已建（@import 不 404）
- ✅ **通用组件已完成（2026-08-30 深夜）**：toast/modal（弹窗+抽屉+确认框）/loading/chart（SVG 折线柱状饼图）/sidebar/header，完整布局已接入 main.js
- ✅ **阶段2 页面已完成**：模型登记台（卡片+筛选+详情抽屉+扫描）+ 显存账本（分布条+进程表+趋势图+Helper）
- ✅ **阶段3 页面已完成（2026-08-30 深夜++）**：场景页（6卡片+当前高亮+独占标签+切换日志+对话态组合pills）+ 队列页（GMae任务提交表单[模型下拉=有workflow模板的comfyui模型]+任务列表[状态色标/取消]+ComfyUI原生队列）
- ✅ **阶段4 页面已完成（2026-08-30 深夜+++）**：门卫页（状态横幅+告警+建议驱逐+驱逐操作+进程明细[受管/桌面/未登记]+强制结束kick）+ 日志页（/api/logs 结构化事件流，级别筛选+事件名翻译）+ 设置页（系统配置/服务状态/门卫登记簿/QoS/账号改密）
- ✅ **后端新增 /api/logs**（读 vram-console.log 尾部，已重启服务生效）
- ✅ **阶段5 完成（2026-08-30 凌晨）**：预演模式（dashboard 🎼 按钮 → 预算引擎弹窗：安全上限/已用/可释放/可用 + 21 模型决策表 + Context 下拉覆盖重算[196K→11G 实测生效]）
- ✅ **前端 v2.0 全部 8 页面完成**：dashboard/scenes/models/vram/queue/guard/logs/settings，浏览器 8 页导航冒烟全过、0 代码错误
- ✅ **一键演示 overlay 完成（2026-08-30 凌晨++）**：components/demo.js + components/demo.css，dashboard 加「🎬 一键演示」按钮。5 幕脚本：①显存秒级释放（真实 /free）②门卫强制驱逐（模拟，不真实 kill）③预算引擎智能决策（真实 /budget）④多模态连续生成（模拟，不真实生成）⑤多场景稳定切换（模拟，不真实启停容器）。控制：开始/暂停/单步/停止/关闭，进度条+计时+当前显存实时刷新。危险操作已模拟保护。
- ✅ **前端 v2.0 全部完成**：8 页面 + 预演模式 + 一键演示，浏览器验证全过、0 代码错误
- ✅ **显存账本页修复（2026-08-31）**：①修复 vram.js 多余 `}` 导致 ESM 模块加载失败、路由未注册、页面白屏（需 .mjs 扩展名强制 ESM 检查才能发现）②修复展开状态自动折叠（renderAll 轮询刷新时保存/恢复展开分组+子行可见性）③显存分布进度条交互（按层级分段：底噪→Ollama→ComfyUI→Fooocus→其他→空闲，点击段/图例展开对应分组+平滑滚动定位+高亮闪烁，Fooocus 运行时自动出现独立紫色段）
- ✅ **显存危险预警第一层完成（2026-08-31）**：后端 /api/status 新增 vram_ledger.danger_level（safe>4GB/warning 2-4GB/danger 1-2GB/critical<1GB）+ free_mb；critical 状态变化时自动记录系统日志 vram_danger_critical；前端顶部状态栏危险等级指示器（黄/橙/红+闪烁动画）；从非 critical 变为 critical 时自动弹出紧急操作弹窗（空闲警告+占用排行列表+单项释放按钮+一键释放全部）；CSS 新增 dangerBlink 关键帧动画
- 📋 **GMae 高负载三层应对策略已讨论确定（2026-08-31）**：第一层=立即预警（已完成），第二层=智能建议+"其他"未归因显存诊断（待做），第三层=自动防死机（待做，需用户授权配置）

### 3. 新会话第一步做什么？
**前端 v2.0 已全部完成**（8 页面 + 预演模式 + 一键演示 overlay）。后续可选项：
- 页面润色/空状态/响应式微调
- 演示脚本内容微调（5 幕的日志文案/预计时间）

**已完成的 8 页面**：dashboard/scenes/models/vram/queue/guard/logs/settings，注册在 main.js registerPages()，导航在 sidebar.js，API 全集在 core/api.js。

**踩坑备忘**：
- registry.comfyui_models 是**数组**；gpu_processes 用 `processes`（受管）+ `desktop_processes`（桌面）+ `unknown_pids`
- 场景切换 POST /api/scene 会真实启停容器；门卫 kick POST /api/guard {action:kick,pid} 会 docker exec kill（protect 自动拒绝）
- 重启 server.py 会清空内存 session → 浏览器需重新登录
- **后端已改2处**：新增 /api/logs；/api/budget 路由改 `self.path.split("?")[0]==` 支持 context query
- **前端已修**：api.budget 双重编码（%→%25）导致 context 覆盖失效，改整体单次 encodeURIComponent
- **helper 启动修复（2026-08-30）**：根因是 `_helper_process_count()` / `_helper_kill_processes()` 的 PowerShell 命令查所有进程，把自身 powershell 进程也算进去（命令行含 'vram-helper' 字符串），导致误报残留进程。修复：加 `-Filter "Name='python.exe'"` 只查 python 进程。同时改进 helper_start：优先直接启动（非管理员也能跑）→失败再 UAC；检查 ShellExecuteW 返回值；启动后轮询确认；残留进程自动清理重试；新增 log_info 函数。config.json 的 helper_token 已从占位符替换为真实 32 位随机 token。
- **显存账本页桌面进程修复（2026-08-30）**：之前桌面进程显存显示'—'且无结束按钮。根因：load() 只调 api.status()，没调 api.desktopVram()；进程表没渲染桌面显存和操作列。修复：vram.js load() 在 helper 运行时额外调 api.desktopVram()；renderProcTable 接收 desktopVram，按 Pid 匹配显示显存；加'操作'列，桌面进程和未登记进程加'结束'按钮（confirm 确认弹窗 + api.desktopKill）。验证：23 行进程、21 个结束按钮、explorer 131MB 等显存正常显示；后端 /api/desktop/kill 实测结束 notepad 成功。注意：helper 非管理员运行时无法结束系统进程（如 explorer），用户进程可正常结束。
- **ESM 模块语法错误检测（2026-08-31）**：vram.js 多余一个 `}` 导致 ESM 动态 import 失败，路由未注册，页面白屏显示"路由 vram 未注册"。关键坑：`node --check vram.js`（CommonJS 模式）**检测不到**，必须用 `.mjs` 扩展名复制后 `node --check` 强制 ESM 解析才能报出具体行号。后续所有 JS 文件修改后必须用 .mjs 方式检查
- **server.py 残留进程（2026-08-31）**：重启 server.py 时发现 23 个旧进程同时运行，导致代码修改不生效（用户访问的是旧进程）。根因：之前用 `Get-Process python | Where-Object CommandLine` 时 PowerShell 的 Get-Process 没有 CommandLine 属性，匹配不到任何进程，旧进程没被停掉。修复：必须用 `Get-CimInstance Win32_Process -Filter "Name='python.exe'" | Where-Object CommandLine -like "*server.py*"` 精确查找并全部停止
- **modal.js open() 用法（2026-08-31）**：`openModal(html, {closable:true})` 是错误用法。正确用法：`openModal({title:'标题', body:'HTML内容', footer:'HTML或DOM', width:'480px'})`，返回的是 `close` 函数（不是 modal 对象），关闭用 `close()`，关闭后需手动设置 dangerModalOpen=false
- **进程级显存明细表重构（2026-08-30）**：用户要求 3 点：①只显示占用显存 > 0 的进程，按大小降序 ②隐藏 0 MB 进程 ③COMFY/Ollama 总占用下钻到模型级，Windows+Docker 打通统一展示。重构 vram.js renderProcTable：统一收集所有显存占用项（Docker 模型组 + Windows 桌面进程 + 未登记进程），仅保留 MB > 0，按显存降序混排。Docker 组（ollama/comfyui）作为可展开/折叠的分组行，下钻显示加载的模型明细（name + 显存）。Fooocus 固定 6.9G。Windows/未登记进程保留结束按钮。新增 vram-group CSS 样式。**关键坑**：`status.comfyui_models` 是对象 `{ok, models, total_vram_gb, note}`（来自 comfy_loaded_models()），不是数组！必须用 `status?.comfyui_models?.models || []` 取模型列表，直接 `.reduce()` 会报 "comfyModels.reduce is not a function" 导致整个进程表崩溃为空。ollama.models 是数组（status.ollama.models），可直接用。验证：29 行 Windows 进程（0 MB 已隐藏）、29 个结束按钮、按显存降序（dwm 555MB→214MB→explorer 122MB...）、当前无模型加载所以无分组行（代码已实现，加载模型后可见）。

### 4. 关键文件路径（按重要性排序）

| 文件 | 作用 | 状态 |
|------|------|------|
| `16gb-ai-studio/vram-console/server.py` | 后端服务（22 个 API 接口），已支持版本切换和静态文件服务 | ✅ 运行中 |
| `16gb-ai-studio/vram-console/web/` | 新前端 v2.0 目录（所有新代码放这里） | 🔨 开发中 |
| `16gb-ai-studio/vram-console/web/js/main.js` | 新前端入口（已接入核心模块+路由+dashboard 阶段1） | ✅ 运行中 |
| `16gb-ai-studio/vram-console/web/css/` | 新前端样式（variables/reset/main） | ✅ 基础版 |
| `16gb-ai-studio/vram-console/legacy/v1-index.html` | 旧版归档（可工作的历史版本，回滚用） | ✅ 已归档 |
| `16gb-ai-studio/vram-console/auth.py` | 认证系统（用户管理、Session、SMTP） | ✅ 完成 |
| `16gb-ai-studio/vram-console/resources/registry.json` | 资源注册表（模型/场景/组合配置） | ✅ 完成 |
| `16gb-ai-studio/docs/调度中心架构与交互设计.md` | 蓝图（设计权威，修改需用户批准） | 📖 参考 |
| `16gb-ai-studio/docs/开发日志.md` | 开发历史（踩坑记录） | 📖 参考 |

### 5. 新前端目录结构（目标）

```
web/
├── index.html              # 入口 HTML（挂载点 + ES Module）
├── css/
│   ├── variables.css       # ✅ 设计令牌（主题色/间距/字体/阴影）
│   ├── reset.css           # ✅ 样式重置
│   ├── main.css            # ✅ 主样式入口（布局/网格/工具类）
│   └── components/         # ⏳ 组件级样式（button/card/modal/sidebar...）
├── js/
│   ├── main.js             # ✅ 应用入口
│   ├── core/               # ⏳ 核心模块（utils/events/api/state/router）
│   ├── components/         # ⏳ 通用组件（sidebar/header/modal/toast/loading/chart）
│   └── pages/              # ⏳ 页面模块（login/dashboard/models/vram/scenes/...）
└── assets/
    └── icons/              # 图标资源
```

### 6. 技术规范（必须遵守）
- **JS**：ES Modules（`import/export`），每个文件独立作用域，禁止全局变量
- **CSS**：CSS 变量 + BEM 命名规范（`.block__element--modifier`），组件级 CSS 文件
- **零构建工具**：不需要 npm/webpack，浏览器原生支持 ES Modules
- **API 调用**：必须通过 `core/api.js`，禁止在页面中直接 fetch
- **状态管理**：必须通过 `core/state.js`，禁止组件间直接传参
- **版本号**：UI 上必须显示版本号（当前 v2.0.0-alpha）

### 7. 如何验证？
1. 启动服务：双击 `桌面\调度中心启动.bat`
2. 访问：http://127.0.0.1:8787/
3. 看到 v2.0.0-alpha 版本号即为新前端
4. 回滚旧版：`set FRONTEND_VERSION=v1` 后重启服务

### 8. 常见坑
- **comfyui/fooocus 容器不存在**：当前 Docker 中这两个容器已被删除，场景切换会快速失败（正常），不要以为是 bug
- **server.py 修改后必须重启**：Python 后端不支持热重载
- **蓝图禁止擅改**：`调度中心架构与交互设计.md` 是设计权威，任何修改必须先经用户同意
- **禁止重复创建看门狗**：唯一看门狗是 `vram-console/watchdog.py`

---

## 📌 当前状态摘要

### 项目身份
- **总项目**：GPU Maestro-显存指挥家（GMae）
- **子项目**：16G-AI-Studio（16GAS，参赛作品）
- **核心引擎**：Prism Engine-棱镜引擎（P-Eng）
- **参赛**：2026 上海开源软件应用创新大赛 — 智算云赛道，个人参赛，截止 10-11
- **⚠️ 工作边界（2026-08-28 主公指示）**：本工作区 `D:\Users\Danny\Documents\GMae_Amanda` 为 GMae **独立推进项目与唯一输出位置**；与 `家庭智能中枢` 主仓库**完全切割**——不回写、不读取（主仓库仅历史参考），所有产物一律落本工作区。

### 系统状态（2026-08-29 收工时）
- **调度中心服务**：✅ 运行中（端口 8787，GPU 空闲 ~13GB）。⚠️ 当前有 2 个 server.py 进程（PID 38332/38196），下次启动前建议清理残留。health ok=False 是因为 ollama 容器未运行（非致命）
- **访问地址**：http://127.0.0.1:8787/（认证方式：邮件+密码+Session 登录；API Token 保留为向后兼容，用于脚本/自动化）
- **认证系统**：✅ 已重构（auth.py，PBKDF2 密码哈希 + Session Cookie + QQ 邮箱 SMTP 验证码 + 7 个认证 API），用户已成功登录。忘记密码/修改密码功能待实测
- **Docker 服务**：OWUI、ComfyUI、Ollama、Immich、SearXNG 正常运行，重启策略 unless-stopped/always
- **看门狗**：唯一看门狗 `vram-console/watchdog.py`（GMae 工作区），已统一登记在 `WATCHDOGS.md`。**铁律：只维护这一套，禁止再搞第二套**
- **桌面启动 bat**：已修复指向 GMae 工作区（之前指向旧工作区家庭智能中枢，且注释 GBK 乱码）
- **UI 重构**：✅ 完成（蓝图第十一章三层界面结构 + 预演模式 + 模型登记台升级 + 状态播报）
- **⚠️ 前端重写进行中（2026-08-30 下午启动）**：
  - 旧版问题：单文件 2214 行、69 全局函数、291 全局变量、无模块化、无作用域隔离
  - 版本隔离：旧版已归档到 `legacy/v1-index.html`，server.py 支持 `FRONTEND_VERSION` 环境变量切换（v2默认/v1回滚）
  - 新架构：ES Modules 模块化，三级架构（core/components/pages），零构建工具
  - 已完成：CSS 变量/重置/主样式、入口 HTML/JS、版本切换机制、静态文件服务
  - 待完成：分 5 阶段重写全部页面（见下方待完成工作）
  - 回滚方法：`set FRONTEND_VERSION=v1` 后重启服务
- **进程账本**：✅ 修复（WSL2 下 nvidia-smi 拿不到 PID 时用容器内 ps 进程名识别兜底，精确匹配 llama-server 不匹配 ollama 主进程）
- **Ollama 模型列表**：✅ 修复（status 新增 installed 字段，区分加载中/已安装，8 个模型含新加的 darkidol-8b/qwythos-9b/defiant-9b）
- **性能问题**：⚠️ /api/status 响应 13-17 秒偏慢（docker exec 串行），待后续并行化+缓存优化
- **watchdog 反复重启根因**：已定位——server 启动后 health 检查超时（"Server failed to come up within 90s"），server 已用 ThreadingHTTPServer 多线程，需观察是否还反复
- **跨硬件 UI 适应性**：✅ 9 条设计约束已记录开发日志（动态显存/GPU型号/多卡/服务检测/响应式/性能降级/配置驱动/跨OS/看门狗跨平台），后续 UI 重构必须遵守
- **误操作**：今日清理进程时误杀 whisper TTS 服务（tts_server.py，PID 22404/7420），如需用请重启
- **QoS 引擎**：✅ 已修复自动切换 bug。原设计 RED 水位时自动调用 combo_switch("0.6b") 拉起 qwen3:0.6b，会干扰用户测试。已改为方案 A：只做监控+告警，不自动切换模型，模型切换由用户手动触发
- **模型登记台校准**：✅ 用家庭智能中枢的模型评测台帐实测数据校准了 11 个 LLM 模型，填充 context_vram（多档位显存）、max_safe_ctx、速度（prefill/gen）、multimodal 等字段，全部标记 vram_verified=true
- **category 字段 bug**：✅ 已修复。自动扫描器早期版本把 7 个新模型的 category 错误设为 "image"，导致 BIG_MODELS 回退到硬编码 4 个模型。已全部改为 "llm"，现在 /api/registry 返回 14 个模型（11 LLM + 2 embedding + 1 reranker）
- **视频模型**：H3 因 52GB OOM 标记不可行 → Wan2.2-TI2V-5B 已跑通（9.31GB FP16，480x480x17帧，峰值~10.9GB）
- **音频模型**：ACE-Step 1.5 Turbo 已测（7.5分）
- **VPS**：唯一 exit node = ai-registry-jp（东京新，52G，Split Tunnel）

---

## ✅ 最近完成的工作

### 本次会话（豆包，2026-08-30 下午，前端重写启动：版本隔离+架构设计+基础框架）
1. **旧版问题修复（重写前紧急修复）**：
   - 场景切换改用 registry.scenes（commit e19510a）
   - 场景切换 120 秒超时保护（commit b274b99）
   - 场景切换关键步骤检查，失败返回 ok=False（commit 083e828）
   - start 失败跳过 wait_ready，避免等待 90 秒（commit 60fad52）
2. **前端重写架构设计**：
   - 技术选型：原生 HTML+CSS+JS (ES Modules)，零构建工具
   - 三级架构：core/（API/状态/路由/事件）→ components/（侧边栏/弹窗/Toast/图表）→ pages/（各功能页面）
   - CSS 规范：CSS 变量 + BEM 命名 + 组件级 CSS 文件
3. **版本隔离（项目管理层面）**：
   - 旧版归档：index.html → legacy/v1-index.html（135KB，可工作的历史版本）
   - 版本切换：server.py 添加 FRONTEND_VERSION 环境变量（v2默认→web/，v1→legacy/）
   - 静态文件服务：server.py 添加 /web/ 路由，支持 CSS/JS/图片/字体
   - 回滚方案：set FRONTEND_VERSION=v1 后重启服务，1 分钟回滚
4. **新前端基础框架（已完成）**：
   - web/css/variables.css - 设计令牌（主题色/间距/字体/阴影）
   - web/css/reset.css - 样式重置
   - web/css/main.css - 主样式入口（布局/网格/工具类）
   - web/index.html - 入口 HTML
   - web/js/main.js - 应用入口（版本号 v2.0.0-alpha + 开发中提示）
5. **文档更新**：开发日志新增 2026-08-30 记录（前端重写启动与版本隔离）
5. **文档更新**：开发日志新增 2026-08-30 记录（前端重写启动与版本隔离）

### 本次会话（豆包，2026-08-30 晚间，前端 v2.0 核心模块 + 阶段1 总览页）
1. **核心模块 5 件套完成**（web/js/core/）：
   - `utils.js`：格式化（显存/时间/相对时间/时长）、防抖/节流、DOM 工具、escapeHtml XSS 防护、unwrap 错误解析
   - `events.js`：事件总线（on/off/once/emit、命名空间、通配符 '*' 监听）
   - `api.js`：统一 API 客户端——封装全部 30+ 后端接口（auth/status/registry/budget/scan/queue/guard/qos/scene/combo/free/service/model/desktop），GET 网络错误重试、POST 不重试、超时控制、401→auth:unauthorized 事件、错误→api:error 事件
   - `state.js`：集中式状态管理（按域组织 subscribe/set/update，ui.loading 快捷方法）
   - `router.js`：哈希路由（#/page?query，register/go/start、权限守卫、404、路由事件）
2. **阶段1 总览页完成**（pages/dashboard.js）：
   - L0 一瞥：显存水位能量条（绿/黄/红状态机）+ 分段占用标签 + 账本提示
   - L1 动作：一键释放（调 /api/free）+ 预演模式（阶段5占位）
   - 统计卡 4 张（GPU/场景/活跃模型/QoS）+ 服务活跃度卡片
   - 10s 轮询刷新 + 页面进入/离开生命周期（onEnter/onLeave）
3. **组件 CSS 基础版**：button/card/sidebar/modal/toast/table/form/chart 全部落盘（main.css @import 不再 404）
4. **服务重启加载新代码**：8787 旧进程（16:09 启动，无 /web/ 路由）→ 杀旧进程，watchdog 自动拉起新 server（PID 32272）
5. **验证通过**：7 个 JS 文件语法（node module 模式）✅；/web/ 静态服务 200 ✅；X-API-Key 调 status/registry/budget/queue 全部 200 ✅；read_html() v2 返回新前端 HTML ✅；登录页渲染正常 ✅
6. **待用户登录后查看主应用**（浏览器端到端：登录 → 新前端 dashboard 渲染，需用户提供密码，本次未登录）
6. **待用户登录后查看主应用**（浏览器端到端：登录 → 新前端 dashboard 渲染，需用户提供密码，本次未登录）

### 本次会话（豆包，2026-08-30 深夜，通用组件 + 完整布局 + 阶段2 页面）
1. **通用组件 6 件完成**（web/js/components/）：
   - `toast.js`：订阅 toast/api:error 事件自动弹提示，手动 show/success/error/info/warn
   - `modal.js`：open（通用弹窗）/confirm（确认框 Promise）/drawer（右侧抽屉），ESC/遮罩关闭
   - `loading.js`：spinner/overlay/skeletonCards/btnLoading
   - `chart.js`：无依赖 SVG 折线/柱状/饼图（走 CSS 主题色）
   - `sidebar.js`：8 页导航（总览/场景/模型/账本/队列/门卫/日志/设置），折叠功能，路由高亮
   - `header.js`：页面标题/主操作插槽/版本号/用户信息/退出登录
2. **完整布局接入**：main.js 构建 sidebar+header+content 三层；router.js 增加 setContainer() 支持渲染到 content 区
3. **阶段2 页面完成**：
   - `pages/models.js` 模型登记台：卡片网格+分类筛选+详情抽屉（vendor/release/detail/vram）+扫描新模型+注册
   - `pages/vram.js` 显存账本：分布条（对话/生成/底噪/空闲）+进程级明细表（受管/桌面/未登记三色）+会话内趋势折线+Helper 启停
4. **验证通过**：15 个 JS 文件语法全过（node module）；模块加载（import 路径/导出）全过；/web/ 静态服务 200；API 认证链路 200
5. **主应用渲染仍需登录**：SESSIONS 在 server 进程内存，无法外部伪造 session；浏览器停在登录页（用户未登录）
6. **浏览器端到端验证通过（用户已登录）**：dashboard（显存水位/统计卡/服务活跃度实时数据）、models（21模型+分类筛选+详情抽屉+ESC关闭）、vram（分布条/趋势折线/进程表/Helper）全部渲染正常，sidebar 路由高亮正确，console 无前端错误
7. **本轮修复 4 处前端 bug**（均已在 web/js 落盘，浏览器已验证）：
   - `router.js`：dispatch 未调用页面 onEnter → 页面数据不加载（核心修复，vram/dashboard 数据驱动）
   - `main.css`：`.page` 残留旧版 `display:none` → 路由单页渲染被隐藏
   - `sidebar.js`：`${buildNav().outerHTML}` 副本导致高亮操作在未挂载节点上 → 改直接 append
   - `dashboard.js`：activity 结构取错（应取 `.services`）；顺带 vram.js 清理 events_emit 死代码改事件总线

---

### 上次会话（豆包，2026-08-30 凌晨，第二阶段优化冲刺：测试集+安全加固+蓝图偏差登记+UI设计参考图）
1. **P0-3：修复函数重复定义**（_estimate_ollama_vram）：
   - server.py 中两处同名定义（按参数×量化系数 vs 按文件大小），后者覆盖前者
   - 合并为单一实现：优先用 /api/tags 实际文件大小+0.8GB 缓冲，失败时回退到模型名参数×量化系数估算
2. **P0-2：补最小自动化测试集**（74 个测试全部通过）：
   - 新增 tests/ 目录：test_registry.py（22个）、test_auth.py（32个）、test_api_contract.py（20个）
   - 新增 run_tests.py 测试运行入口（支持全部/单个模块、详细/安静模式）
   - 零依赖：全部使用 Python 标准库 unittest
   - 测试结果：74 tests, 0 failures, 0 errors，耗时约 31 秒
3. **P1-2：彻底消灭 shell=True**：
   - 7 处业务逻辑中的 run() 调用全部改为 run_args() 参数数组
   - run_ps1() 改为直接使用 subprocess.run() 参数数组
   - **删除 run() 函数**（shell=True 已彻底消灭）
4. **安全增强：_safe_model_name 增加路径穿越检测**：
   - 测试发现原正则允许 / 字符，../../../etc/passwd 能通过校验
   - 增加两道检查：禁止包含 '..'（路径穿越）、禁止以 / 或 \ 开头（绝对路径）
5. **P1-6：新增代码↔蓝图偏差登记段**（项目进度跟踪.md 第八章）：
   - 登记 8 项已知偏差（D-01 ~ D-08），每项含蓝图章节、蓝图描述、代码实际、偏差原因、批准人、登记日期
   - 目的：受「蓝图禁止擅改」铁律约束，代码与蓝图不一致时不静默修改，而是登记可追溯
6. **数据修复：registry.json 数据质量**：
   - 修正 3 个 embedding/reranker 模型 vram_gb=0 → 1.5GB
   - 修正 7 个模型缺少 ctx 字段（按 default_ctx 或默认 8192 补上）
7. **UI 大改：生成 Immich 风格设计参考图**（3 张高保真 UI mockup，1920x1080）：
   - 总览页 Dashboard：左侧导航栏 + 4 统计卡片 + 显存水位状态机 + 事件时间线
   - 模型登记台：卡片网格布局 + 模型图标/名称/极短简介/显存标签 + 右侧详情抽屉
   - 显存账本：显存总览分段条形图 + 进程级明细表格 + 显存历史趋势折线图
   - 保存位置：docs/assets/ui-design/
   - **待用户确认设计方向后，再全面改造 index.html**
8. **认证系统**（前半夜已完成，本轮一并记录）：
   - 新增 auth.py：用户管理 + PBKDF2 密码哈希 + Session 管理 + QQ 邮箱 SMTP + 6 位验证码
   - server.py 集成：_check_auth 支持 Session Cookie + API Token 双认证
   - 新增 7 个认证 API：status/setup/login/logout/forgot/reset/change-password
   - 内置登录页：登录/首次设置/忘记密码 三个 Tab
   - users.json 加入 .gitignore
9. **Git 提交**：
   - 认证系统：c894064
   - 第二阶段优化：a53c4ff（74 测试 + shell=True 消灭 + 偏差登记 + UI 参考图 + 数据修复）
   - 已推送到 Gitee 主分支

### 上次会话（豆包，2026-08-29 深夜，第二轮测试+registry校准+QoS修复）
1. **第二轮 ollama 大规模测试观察**（30分钟，验证 6 项显存账本改进）：
   - P0-1 显存账本双源化：✅ 8 次捕捉到 ollama ps 的 15 秒延迟窗口，loading 状态准确
   - P0-3 自动扫描器：✅ 补登记 6 个新模型，登记数 8→14
   - P1-5 QoS 利用率修正：✅ YELLOW+高利用→升级RED 生效
   - P1-4 多模型并发：✅ 5 次 2 模型并发，账本差异 <0.7GB
   - P2-6 加载进度：✅ 每次 loading 显示进度和 ETA
   - 发现 4 个待优化问题：小模型大 context 账本差异、QoS utilization 字段未同步、小模型显存估算偏高、QoS 水位偶尔异常
   - 产物：`docs/观察报告_第二轮_6项改进验证_20260829.md`
2. **模型登记台实测数据校准**：
   - 读取家庭智能中枢的 `模型评测台帐.md` 5.1 节，提取 11 个 LLM 的实测显存/context/速度数据
   - 更新 registry.json：填充 context_vram（多档位）、max_safe_ctx、max_safe_vram、speed_prefill_tok_s、speed_gen_tok_s、multimodal、notes 等字段
   - 全部 11 个 LLM 标记 vram_verified=true，移除 auto_registered 标记
3. **修复 category 字段 bug**：
   - 自动扫描器早期版本把 7 个新模型（defiant/gemma/minicpm/qwen3.5:0.8b 等）的 category 错误设为 "image"
   - 导致 BIG_MODELS 过滤后为空，回退到硬编码 4 个模型，/api/registry 返回 0
   - 已全部修复为 "llm"，现在 /api/registry 返回 14 个模型
4. **🔴 发现并修复 QoS 自动切换严重 bug**（主公反馈：测试时显存快满会自动拉起 qwen3:0.6b）：
   - 根因：qos_enforce() 在 RED 水位时自动调用 combo_switch("0.6b")，GREEN 时自动恢复 9b
   - 危害：干扰用户主动测试、可能加剧 OOM、自动恢复振荡
   - 修复：方案 A，完全移除自动切换逻辑，QoS 只做监控+告警（RED 时设置 msg 建议手动处理，记录 qos_alert 日志），模型切换完全由用户手动触发
   - 技术决策：调度中心是管理工具不是自动代理，激进的自动操作应该由用户确认
5. **开发日志更新**：追加第二轮测试记录（验证结果+4个待优化问题+实测数据）

### 上次会话（豆包，2026-08-29，UI重构+进程账本+看门狗清理）
1. **桌面 bat 修复**：桌面「调度中心启动.bat」cd /d 指向旧工作区（家庭智能中枢），违反切割原则；注释 GBK 乱码。重写指向 GMae_Amanda 工作区，注释改英文消除编码坑
2. **UI 全面重构**（蓝图第十一章「人机交互界面设计」落地）：
   - 三层界面结构：sticky header 主指标条 + 状态播报行 + 动作层大按钮（一键全释放/切ComfyUI/切对话态/预演模式/Panic）
   - 🔮 预演模式（大赛杀手锏）：新弹窗，基于预算引擎列出所有模型「✅可跑/⏳释放X G/⛔差Y G」，选中显示"将释放什么"→确认才执行
   - 模型登记台升级：短名+类别图标+Hover显示全称（出品方·发布时间）+极短简介+点击滑出详情抽屉（全称/出品方/发布时间/详细介绍/显存备注/独占标记）。14个模型补全元数据
   - 10个折叠区段收进可折叠面板（少即是多）
3. **进程账本修复**（主公核心要求）：
   - WSL2 下容器内 nvidia-smi compute-apps 拿不到 PID（[N/A]），导致进程账本为空
   - 加 ps_fallback 兜底：nvidia-smi 拿不到时用容器内 ps 进程名识别 GPU 进程
   - 精确匹配 "llama-server"（不匹配 ollama 主进程，因为 "ollama" 包含 "llama"）
   - _gpu_app_pids() 接收 names 参数减少重复 docker 调用
4. **Ollama 模型列表实时化**：status 新增 installed 字段（/api/tags，8个模型），区分加载中（/api/ps）和已安装，解决用户反馈"新模型不显示"
5. **watchdog 端口配置**：watchdog.py PORT 改为读环境变量 VRAM_CONSOLE_PORT（和 server.py 一致）；曾尝试换端口 8790 避免冲突，最终主公要求只留一套，改回默认 8787
6. **看门狗统一清理**：杀掉所有 vram-console 进程（含多个孤儿 server/watchdog），只留 GMae 工作区一套 watchdog.py → server.py。主公铁律：禁止再搞第二套
7. **跨硬件 UI 适应性设计要求**（主公提出）：9条约束已记录开发日志——动态显存/GPU型号适配/多卡预留/服务可用性检测/响应式布局/性能降级/配置驱动/跨OS/看门狗跨平台
8. **旧工作区统一看门狗机制调研**：翻了家庭智能中枢的 WATCHDOGS.md + 开发日志，确认机制为"唯一看门狗+统一登记册+统一管理脚本+防失控重启策略"，GMae 工作区已继承此机制
9. **实演练验证**：趁另一个会话测试 LLM 时验证进程账本/模型列表/显存监控，发现并修复了多个问题

### 上次会话（豆包，2026-08-27 晚）
1. **Wan2.2-TI2V-5B 部署**：通过 hf-mirror 国内直连下载 9.31GB FP16 模型，ComfyUI 原生支持，UMT5-XXL FP8 文本编码器已有
2. **Wan2.2 VAE 关键坑**：5B TI2V 版本必须用 `wan2.2_vae.safetensors`（1.3GB，48通道），不能用 `wan_2.1_vae.safetensors`（242MB，16通道），否则 VAE 解码报 "expected 16 channels but got 48"。已下载正确 VAE 并重新测试
3. **Wan2.2 官方工作流**：ComfyUI 内置模板（Workflow → Browse Templates → Video → "Wan 2.2 5B Video Generation"），5B版本用 Wan22ImageToVideoLatent 节点。工作流文件：`D:\docker\comfyui\workspace\user\default\workflows\Wan22_T2V_Test.json`
4. **EasyAIVid 编译完成**：克隆源码，安装 VS Build Tools with C++，配置 Windows 安全中心排除项，Tauri release 编译成功（ai-video-studio.exe 6.69MB，位置 EasyAIVid/src-tauri/target/release/），交别的会话测试
5. **模型评测**：FLUX 测完；Hunyuan15 测试 1分（没法看）；ACEStep15 测试 7.5分（节奏音质不错，轻微断音）；**Wan2.2-TI2V-5B 已跑通**（480x480x17帧，峰值显存~10.9GB）
6. **下载 Skill 智能路由**：国内模型（阿里 Wan2.2 等）走 hf-mirror 直连 + aria2c 16线程，国外模型走 VPS 中转
7. **输出产物目录统一**：`D:\Users\Danny\Documents\家庭智能中枢\outputs`（images/videos/audio 子目录）
8. **代码仓库整理（另一豆包会话）**：清理 scripts/ 目录（删除37个VPS临时脚本+2个pull脚本，保留7个核心脚本）、删除watchdog.vbs、更新README（H3状态/截图/项目结构）、提交54个文件并推送到 GitHub+Gitee
9. **文档全面整理（另一豆包会话）**：删除3个空壳指针文件、归档10个历史文档（DSH系列5+已完成方案5）、整合H3文档群（4→2）、修正memory.md为纯指针、重写docs/README.md索引、东京旧VPS下线更新19个文件

### 接手会话（豆包，2026-08-27 接手）
1. **接盘体检**：三方对齐（记忆文档 ↔ 代码现状 ↔ 运行状态）——确认调度中心运行中（显存空闲14.8/16GB）、registry.json 实际在 `vram-console/resources/`（非文档暗示位置）、前端仍含 BUDGET_ROWS/SCENES 硬编码31处（印证 Step3 未做）、server.py 无 compute-apps//free/WebSocket（印证 Step1/2/7 待做）
2. **未落盘内容补录**（前序发散讨论遗留）：
   - 新建 `docs/运营蓝图.md`：高声量运营打法（声量=质量×节点×覆盖 / 一鱼多吃 / 渠道矩阵 / 引爆逻辑 / AI分工 / 节奏），4 决策点已确认
   - 新建 `docs/定稿决议清单.md`：所有定稿决议汇总（运营O-1~4 ✅ / E9日志脱敏 4 点 ✅ / A1承诺边界+A2信任+B3首次体验 讨论稿待确认 / B4曝光+C1+C2+D1+D2+9话题 待补）
   - 更新 `docs/运营点子库.md`：加指向运营蓝图的战略层指针
3. **注意**：定稿决议清单中 A1/A2/B3 为讨论稿（豆包观点），待 Danny 确认后才并入设计文档

### 上次会话（豆包，2026-08-26 晚）
1. **调度中心 v2 修复**：对齐显存管理最高指南，修复 4 个问题（H3 场景、BIG_MODELS 动态化、docker exec 适配、显存预检）
2. **P0 稳定性实施**：
   - 结构化日志系统（`logs/vram-console.log`，按天轮转）
   - 看门狗自动重启（`watchdog.py`，崩溃 5 秒后重启）
   - 健康检查 API（`/api/health`）
3. **看门狗统一管理**：
   - 清理 3 个废弃启动脚本
   - 创建 `WATCHDOGS.md` 统一登记册
   - 统一管理脚本（start/stop/status）
   - 修复开机自启项
4. **记忆系统重构**：
   - 根目录创建 `AGENTS.md`（总入口）
   - 三层指针体系（GMae 总纲 → 16GAS 蓝图 → 模块文档）
   - 本工作交接文档

### 更早完成
- ollama 容器化（东京 VPS 中转下载，3.2GB 镜像备份在 `_installers/ollama.tar`）
- ~~新加坡 VPS exit node 配置（149.28.134.107，比东京快 3-4 倍）~~ ❌ 已下线（2026-08-26）
- ~~东京旧 VPS ai-exit-jp（207.148.107.141 / 100.81.128.51，23G）~~ ❌ 已下线（2026-08-27），当前唯一 exit node 为东京新 ai-registry-jp（167.179.66.179 / 100.126.118.93，52G，Split Tunnel）
- 架构蓝图 v1.0（15 章设计框架）
- 项目命名确定（GMae / P-Eng / 16GAS）

---

## 🔄 进行中 / 待处理任务

### 高优先级
1. **UI 设计方向确认** — 3 张 Immich 风格参考图已生成（docs/assets/ui-design/），待用户确认后全面改造 index.html
2. **认证系统端到端测试** — 用户已登录成功，忘记密码（SMTP 验证码）/修改密码功能待实测
3. **开源大赛报名材料** — 截止 10-11：
   - ✅ SDXL 效果图、Flux 效果图、Music3 音乐、作品介绍文档/PDF、README 重写
   - ⏳ 视频效果素材（Wan2.2-TI2V-5B 已跑通，需生成高质量参赛素材）
   - 📋 演示视频录制（方案已确认：标语 One GPU, Infinite Models，5分钟完整版+3分钟展示版，纯字幕+配乐，一键演示模式）、仓库改 Public
4. **/api/status 性能优化** — 当前响应偏慢（docker exec 串行），需并行化+缓存+减少重复调用

### 中优先级
5. **前端去硬编码（P1-1）** — SCENES/BUDGET_ROWS/COMBOS 改为从 /api/registry 动态渲染，兑现"配置驱动"承诺
6. **看门狗精细化（P1-3）** — watchdog 升级为"端口 + /api/health"双探测；stop.bat 改为按命令行精确杀 PID（杜绝误杀 whisper 事故）
7. **凭据治理（P1-5）** — config.json 的 helper_token 改用环境变量
8. **registry 数据健康度（P1-7）** — 自动扫描器新增"待确认"状态，清理文件名型 id 和 auto: 动态条目

### 中优先级
5. **UI 重构用户反馈迭代** — 新版 UI 已上线（三层结构+预演+登记台升级），待主公实际使用后反馈调整
6. **进程账本高负载验证** — ps_fallback 兜底已实现，待有模型加载时验证显存数据准确性（当前无模型加载，进程账本可能为空）
7. **P3：任务队列 + 进度 API** — 串行化生成任务，进度反馈
8. **ComfyUI WebSocket 实时事件** — 蓝图要求，待接入

### 低优先级
9. gpu_release.ps1 模型列表硬编码修复
10. STT 服务重启（今日误杀）
11. 导出 ComfyUI 镜像 tar（新机器部署用）
12. 统一 docker-compose.yml（产品化阶段）

---

## ⚠️ 注意事项 / 已知坑

### 绝对禁令
1. **禁止 `docker compose up -d comfyui`** — 会清空容器内所有改动（torch/SageAttention/自定义节点）
2. **禁止显存打满** — 16GB 卡打满 = 系统死机，生成前必须释放到 <4GB
3. **禁止重复创建看门狗** — 唯一看门狗是 `watchdog.py`，修改前先读 `WATCHDOGS.md`
4. **禁止用 pythonw.exe 启动 watchdog.py** — subprocess 不稳定，用 python.exe + 最小化窗口
5. **禁止 home server 长期挂 exit node** — 仅下载大文件时临时启用
6. **🚫 禁止擅改蓝图（主公铁律，2026-08-28）** — `16gb-ai-studio/docs/调度中心架构与交互设计.md` 是设计权威，任何修改必须先经主公同意；不允许"顺手修正"式擅改（2026-08-28 曾擅改 3 处被主公纠正，已回滚）

### 环境限制
- **Agent 工具调用的后台进程会被沙箱清理** — 启动持久化服务需用户手动操作或依赖开机自启（今日多次踩坑：Start-Process 启动的 watchdog/server 会话结束后被清理）
- **python.exe 路径**：`C:\Users\Danny\AppData\Local\Doubao\User Data\sandbox_runtime\bases\9f6d27f23933fb44a3a1c728c88a5ce4\python\python.exe`（Doubao 沙箱，可能变化）
- **VBS 启动不稳定** — 推荐用 `run_watchdog.bat`（自动找 python 路径）+ cmd 隐藏窗口

### 2026-08-30 新发现的坑
- **Edit 工具处理含反斜杠的字符串容易失败**：改用 Python 脚本文件更可靠
- **PowerShell 内联 Python 代码中的引号转义复杂**：写临时 .py 文件执行更稳妥，执行后删除
- **_safe_model_name 原正则允许路径穿越**：原正则 ^[A-Za-z0-9._:/\-]+$ 允许 / 字符，导致 ../../../etc/passwd 能通过校验。已增加 '..' 和绝对路径开头检测
- **registry.json 数据质量需持续维护**：自动扫描器可能产生 vram_gb=0、缺 ctx 字段、category 错误等问题，测试集可辅助发现
- **认证系统 users.json 不入库**：已加入 .gitignore，新机器部署时需首次设置管理员账户

### 2026-08-29 新发现的坑
- **WSL2 容器内 nvidia-smi 拿不到进程显存**：compute-apps 返回 [N/A]/[Not Found]，进程账本必须用 ps_fallback 兜底（容器内 ps 进程名识别 + 服务自报显存）
- **"ollama" 包含 "llama"**：进程名匹配必须用 "llama-server" 精确匹配，不能用 "llama" in comm（会匹配 ollama serve 主进程）
- **watchdog 反复重启根因**：server 启动后 /api/health 检查超时（"Server failed to come up within 90s"），server 必须用 ThreadingHTTPServer 多线程，避免慢请求阻塞 health
- **修改 server.py 后必须杀真正的 8787 进程**：watchdog 可能启动多个孤儿进程，杀错了不会生效，必须确认 8787 监听 PID 的启动时间在代码修改之后
- **桌面 bat 必须指向 GMae 工作区**：之前指向旧工作区家庭智能中枢，导致用户双击后启动旧 server 抢占 8787

### 历史踩坑（详见开发日志）
- aria2 多线程下载大文件会内容损坏（大小对但缺 manifest.json）
- 旧进程不退出会导致测试旧代码（修改后必须杀干净所有 python* 进程）
- ~~新加坡 exit node 比东京快 3-4 倍，下载优先用新加坡~~ ❌ 新加坡已下线（2026-08-26）
- ~~东京旧 ai-exit-jp（207.148.107.141，~280ms）~~ ❌ 已下线（2026-08-27），当前唯一 exit node 为东京新 ai-registry-jp（167.179.66.179，Split Tunnel）
- ollama 镜像备份：`_installers/ollama.tar`（3.2GB），新机器直接 docker load
- **Wan2.2 VAE 坑（2026-08-27）**：5B TI2V 版本必须用 `wan2.2_vae.safetensors`（1.3GB，48通道），不能用 `wan_2.1_vae.safetensors`（242MB，16通道）。14B T2V 版本才用 wan_2.1_vae。用错会报 "expected 16 channels but got 48 channels"
- **Wan2.2 工作流**：5B版本用 `Wan22ImageToVideoLatent` 节点（输出48通道），14B T2V版本用 `EmptyHunyuanLatentVideo`。官方模板在 ComfyUI → Workflow → Browse Templates → Video

---

## 📂 关键文件位置

| 文件 | 位置 | 用途 |
|------|------|------|
| 建设方案总纲 v3.0 | `docs/家庭智能中枢建设方案_总纲_v3.0.md` | GMae 最高权威 |
| 工作交接（本文件） | `工作交接.md`（根目录） | 跨 Agent 衔接 |
| 调度中心架构与交互设计 | `16gb-ai-studio/docs/调度中心架构与交互设计.md` | 16GAS 设计框架 |
| 项目进度跟踪 | `16gb-ai-studio/docs/项目进度跟踪.md` | 任务状态 |
| 开发日志 | `16gb-ai-studio/docs/开发日志.md` | 踩坑记录 |
| 看门狗登记册 | `16gb-ai-studio/vram-console/WATCHDOGS.md` | 自启动管理 |
| 显存管理最高指南 | `docs/显存管理最高指南.md` | 显存调度规则 |
| 16GAS 项目记忆 | `16gb-ai-studio/AGENTS.md` | 子项目详情 |

---

## 🎯 下一个 Agent 建议步骤

1. **先读本文件** → 了解当前状态
2. **读根目录 AGENTS.md** → 记忆系统架构和禁令
3. **读总纲 v3.0** → 项目整体方向
4. **确认服务状态**：双击 `status.bat` 或 `curl http://127.0.0.1:8787/api/health`
5. **如服务未启动**：告知用户双击 `start.bat`
6. **根据进度跟踪选择下一个任务**：建议从 P1（registry.json）开始，或先准备大赛材料

---

## 📝 交接记录历史

| 日期 | 交接者 | 接收者 | 摘要 |
|------|--------|--------|------|
| 2026-08-29（收工） | 豆包 | （待接） | **UI全面重构**（蓝图第十一章三层结构+预演模式+登记台升级+状态播报）；**进程账本修复**（WSL2 nvidia-smi拿不到PID→ps_fallback兜底+llama-server精确匹配）；**Ollama模型列表实时化**（installed字段区分加载中/已安装8个模型）；**桌面bat修复**（指向GMae工作区+消除乱码）；**看门狗统一清理**（只留GMae一套，禁止再搞第二套）；**跨硬件UI适应性9条设计约束**；发现/api/status响应13-17秒偏慢待优化；watchdog反复重启根因已定位（health超时）；误杀whisper TTS服务需重启；server当前未运行需用户手动双击桌面bat启动 |
| 2026-08-27（接手） | 豆包 | （本会话） | **接手本会话**：完成接盘体检（文档↔代码↔运行三方对齐，确认 P0/P1 完成、Step1/2/7 待做、前端仍硬编码）；补录前序未落盘内容——运营蓝图（新建 docs/运营蓝图.md，4决策点✅）、定稿决议清单（新建 docs/定稿决议清单.md，含 E9 日志脱敏4点✅ + A1/A2/B3讨论稿待确认 + B4/C/D/9话题待补）、运营点子库加指针；更新工作交接 |
| 2026-08-27（晚） | 豆包 | （待接） | **Wan2.2-TI2V-5B跑通**(9.31GB FP16, 480x480x17帧, 峰值~10.9GB, 输出outputs/videos/Wan22_test_001.mp4)；发现VAE坑(5B必须用wan2.2_vae 1.3GB 48通道)；**EasyAIVid编译完成**(ai-video-studio.exe 6.69MB, 交别的会话测试)；模型评测FLUX完/Hunyuan15=1分/ACEStep15=7.5分；下载Skill智能路由 |
| 2026-08-27 | 豆包 | （待接） | Flux效果图+Fooocus对比、model-benchmark Skill v2.0、作品介绍PDF、东京旧VPS下线更新(19文件)、文档全面整理(删3/归档10/整合H3/重写索引)、大赛材料6/9完成 |
| 2026-08-26 | 豆包 | （待接） | 新加坡 VPS 下线，批量更新所有文档/脚本中的 Tailscale 配置引用（13个文件）；P0 完成，看门狗统一管理，记忆系统重构，待启动服务 + P1/大赛材料 |

---

*每个 Agent 结束工作前必须更新此文件。更新时在"交接记录历史"中添加一行。*


---

## 📋 交接记录历史

| 日期 | 更新者 | 主要工作 | 关键状态 |
|------|--------|---------|---------|
| 2026-08-29 深夜 | 豆包 | 第二轮测试验证+registry校准+QoS自动切换bug修复 | 调度中心运行中，11个LLM已校准，QoS只监控不自动切换 |
| 2026-08-30 下午 | 豆包 | 前端重写启动：版本隔离+架构设计+基础框架+场景切换4项修复 | 旧版归档legacy/，v2新前端web/开发中，FRONTEND_VERSION可切换回滚 |
| 2026-08-30 晚间 | 豆包 | 前端v2.0核心模块5件套+阶段1总览页+组件CSS骨架 | 核心模块/API封装/dashboard完成，服务已重启加载新代码，主应用待登录验证 |
| 2026-08-30 深夜 | 豆包 | 通用组件6件+完整布局+阶段2页面（模型/账本） | 15文件语法/加载/静态/API全过，阶段3待做，主应用待登录查看 |
| 2026-08-30 深夜+ | 豆包 | 浏览器端到端验证+修复4处bug（router onEnter/.page/高亮/activity） | 三页面渲染+数据+抽屉+导航全过，阶段3待做 |
| 2026-08-30 深夜++ | 豆包 | 阶段3页面（场景+队列） | scenes/queue渲染+模型下拉全过，真实场景切换未测（避免动容器），阶段4待做 |
| 2026-08-30 深夜+++ | 豆包 | 阶段4（门卫/日志/设置）+后端/api/logs | 三页渲染+筛选+进程表全过，8页面完成，阶段5收尾待做 |
| 2026-08-30 凌晨 | 豆包 | 阶段5收尾：预演模式+Context覆盖 | 196K→11G实测生效，8页冒烟全过0错误，前端v2.0全部完成 |
| 2026-08-30 凌晨++ | 豆包 | 一键演示 overlay | 5幕脚本+开始/暂停/单步/停止/关闭，危险操作模拟保护，0错误 |
| 2026-08-31 | DSH | 高负载三层应对策略全部落地：第二层确认就位（/api/advice + 账本页智能建议卡片）；第三层自动防死机完成（分级释放 L1 卸载模型/L2 ComfyUI释放/L3 停Fooocus × conservative/standard/aggressive × critical/danger/warning，默认关闭需用户授权，永不杀桌面/protect 容器，全量审计+toast）；QoS emergency 改受第三层授权闸门；**全系统看门狗检查**（结论：GMae watchdog/server 未运行需恢复；WorkBuddy pythonw×4 + TTS pythonw×2 正常；计划任务无 watchdog 项；Docker 容器 OWUI/Immich/SearXNG/Caddy/STT 在线，Ollama/ComfyUI/Fooocus 离线） | 三层策略✅；server.py 已改 3 处+2 API；98 测试 93 过（5 auth 失败=沙箱 TEMP 不可写，与改动无关）；**待办：重启调度中心+看门狗验证，浏览器验证设置页新卡片** |
| 2026-08-31 | 豆包 | 恢复调度中心（看门狗+server 已运行）；验证三层策略后端全部生效（/api/advice + /api/auto-protect/status + danger_level）；/api/status 冷路径性能优化：_helper_health timeout 2s→0.5s（Windows 下未运行端口连接挂起 10035），冷路径 2007ms→681ms，98/98 测试全过，开发日志已记录 |
| 2026-08-31 | 豆包 | Skill 资产检查：三个技能根+主仓库均无正式 Skill 实体，model-benchmark-suite 沉淀为 docs/models/ 方法论文档体系；协同技巧学习（与主公协作沟通要求.md + 踩坑）；主公批准将其封装为正式 Skill 到 .user_skills，进行中 |
| 2026-08-31 | 豆包 | model-benchmark-suite 封装完成（.user_skills）：SKILL.md + references×2 + scripts/ollama_bench.py（已实测验证 qwen3.5:9b 显存/速度） |


---

## 📌 前端重写待完成工作（v2.0 模块化前端）

### 核心模块（✅ 已完成 2026-08-30 晚间）
- [x] `core/utils.js` - 工具函数（格式化、防抖节流、DOM操作）
- [x] `core/events.js` - 事件总线（组件间通信，on/off/once/emit/通配符）
- [x] `core/api.js` - 统一 API 客户端（全部后端接口封装、错误处理、重试、超时、401事件）
- [x] `core/state.js` - 集中式状态管理（subscribe/notify 模式，按域组织）
- [x] `core/router.js` - 哈希路由（页面切换、权限守卫、参数解析）
- [x] `pages/dashboard.js` - 阶段1 总览页（L0 显存水位+L1 动作+统计卡+10s 轮询）

### 通用组件
- [ ] `components/sidebar.js` - 左侧导航栏（参考 Immich 风格）
- [ ] `components/header.js` - 顶部栏（用户信息、设置、版本号）
- [ ] `components/modal.js` - 弹窗/抽屉（模型详情、设置）
- [ ] `components/toast.js` - Toast 通知（操作成功/失败提示）
- [ ] `components/loading.js` - 加载状态（骨架屏、Spinner）
- [ ] `components/chart.js` - 图表组件（显存趋势、饼图、柱状图）

### 页面重写（分5阶段，每阶段端到端测试通过）
- [ ] **阶段1**：登录页 + 总览页（Dashboard）
- [ ] **阶段2**：模型登记台 + 显存账本
- [ ] **阶段3**：场景切换 + 任务队列
- [ ] **阶段4**：门卫 + 桌面进程 + 操作日志
- [ ] **阶段5**：预演模式 + 一键演示

### 验收标准
- 每个页面端到端测试通过（所有按钮功能正常）
- 无全局变量污染（所有模块通过 import/export）
- CSS 无全局样式冲突（BEM 命名 + 组件级）
- 错误处理完善（API 失败、网络异常、加载状态）
- 版本号显示在 UI 上（v2.0.x）
