# 工作交接文档

> **用途**：跨 Agent（豆包/WorkBuddy/DSH）、跨会话的工作衔接。每个 Agent 开工先读本文件，收工必须更新。
> **结构规范**：见 `docs/跨会话多Agent工程一致性方法论.md`（四段式 + 任务锁，单文件 ≤200 行）。
> **历史归档**：`工作交接_历史归档_20260901.md`（2026-08-30 及之前）。

---

## 〇、任务锁（开工前必看）

- **当前任务拥有者**：豆包
- **当前任务**：引入 Ponytail+Archify 开源 Skill、全仓库死代码清理、调度中心架构图生成（已完成）
- **开始时间**：2026-09-03
- **规则**：同一文件同一时间只允许一个 Agent 修改；接手时若此处是其他 Agent，先确认不冲突再开工；收工写「无」。

---

## 一、当前焦点（不超过 5 条）

- **前端 100% 功能整改 Day1-7 已全部完成**（09-03）：api.js 扩到 41 方法；设置/任务队列/场景/诊断/模型台/门卫/总览/账本/审计/告警 10 页全部动态化；app.js 加全局错误边界。
- **pages.js 已拆分**为 11 个文件（pages 入口 + dashboard/diagnose/alerts/models/vram/scenes/queue/guard/audit/settings），index.html 加载顺序已更新，原文件备份 pages.js.bak。
- **系统拓扑页 5 层重构第一阶段已完成**（09-03）：新增 `core/system_info.py`（CPU/内存/磁盘/网络/OS/WSL/Docker/Python 探测）、`GET /api/system/info`、`engine/topology.py` 从 4 层扩为 5 层（硬件/平台/容器/模型/任务）+ 容器 6 类折叠；前端 `web/js/topology.js` 重写为纵向树。已装 psutil，后端实测 5 层数据正常。
- **导航栏显存条已修复**（#header-vram-fill 宽度+颜色，错误改 console.error）。
- **工程治理**：确定 MIT 许可证（根目录+子项目 LICENSE）、建 THIRD_PARTY.md、建跨会话一致性方法论、开发日志已按月归档（主文件仅留 9 月）。
- **显存页·ComfyUI 模型级显存分解已完成**（09-04）：让 ComfyUI 像 Ollama 一样按模型显示占用。改动 4 文件——①`clients/comfyui_client.py`：修 system_stats 的 devices[0] bug、重写 history 节点解析（健壮定位节点 dict + LOADER_SPEC 19 种 Loader→类别，输出 models+model_kinds）；②`services/comfy.py`：新增 `_comfy_file_index`(docker exec `readlink -f`+`find -printf` 读容器内模型字节、60s 缓存，跨机器不写死路径)/`_match_model_size`/`comfy_model_breakdown`（以 /system_stats 的 torch 实测为锚，history 定模型、文件大小估显存，Σ模型+框架开销=torch 实测；模型合计超 torch+300MB 判为无活跃模型回退单行，防容器重启后 history 残留虚报）；③`gpu/monitor.py` comfyui 段：逐模型 append（pid=comfyui-<名>、kind 字段）+框架/CUDA/缓存 overhead 行，无模型回退单行；④`web/js/pages/vram.js`：类型徽标显示 UNet/VAE/文本编码器等、修复进程名"comfyui (comfyui:..)"重复、估算值标(估)。**固有约束**：ComfyUI/torch 不暴露每模型真实显存，此为"最近工作流+文件大小"最佳估算且与 torch 总量守恒；当前容器无活跃模型时只显示一行框架占用，跑一次工作流后即逐模型展开。已编译/浏览器验证无错。
- **另修**（09-04）：进程明细"停止"操作失败——api.js serviceAction 字段适配 name、scene.py/docker.py 操作白名单补 ollama、删 dashboard.js 重复的简陋 _serviceAction 收敛到 pages.js、vram.js `_stopGpuProcess` 按进程分发（ollama 走 /api/model 精准卸载、comfy/fooocus 停容器）；9 页快速切页"Cannot set properties of null"异步竞态已加路由/挂载守卫。
- **排查备忘**：curl 直测 /api/status 须取响应 `data` 字段（v1 包装 {ok,data,error,meta}），顶层无 gpu；前端 api.js 已自动解包，勿被顶层 None 误导（本次一度误判后端故障）。

---

## 二、下一步（原子任务，标注对应文件）

- [ ] 等主公确认拓扑页实际效果，按反馈优化布局/节点显示（`web/js/topology.js`、`engine/topology.py`）
- [ ] 继续逐页 debug（拓扑页之后按侧边栏顺序推进，一次只聚焦主公点名的问题，基于现有后端平台、不写独立胶水代码）
- [ ] 联调任务队列预算预检 admission 参数（见 Open Issues #1，`web/js/pages/queue.js` ↔ `engine/admission_gate.py`）
- [ ] 排查 Ollama models 列表为空（拓扑模型层无节点，`services/status.py` 的 ollama 探测）
- [ ] S6 大赛演示整合（演示脚本/截图/视频/README，仓库改 Public，截止 10-11）

---

## 三、活跃问题 Open Issues（未关闭的坑，防止遗忘）

1. ~~**任务队列预算预检报错**~~ ✅ **已修复（09-04）**：根因是 `api/route_helpers.py:build_gate_context()` 取 `REGISTRY.get("models", {})`，但 registry.json 无顶层 models 键（模型在 ollama.models/comfyui.models），ctx.registry_models 恒为空导致所有已登记模型被 R7 误判为"未登记"，前端预算预检永远报错、提交按钮禁用。修复为从 ollama.models+comfyui.models 构建。3 用例验证通过（SDXL/qwen 通过，未登记模型仍拦截）。前端 category/embedding 过滤已在 queue.js 中正确实现。
2. ~~**拓扑模型层为空**~~ ✅ **已修复（09-04）**：根因是 `engine/topology.py:_build_model_layer()` 只读 `ollama.models`（运行中模型，对应 /api/ps，无模型加载时为空），未回退到 `ollama.installed`（已安装模型，14个，字符串列表）。修复：运行中为空时回退到已安装列表并标记 idle 状态；ComfyUI 无活跃模型但 torch_vram_used_mb>1024 时显示"框架占用"节点。验证：拓扑 API 模型层从 0→14 节点（全部 idle/未加载），前端 neutral 徽章正确渲染。
3. ~~**拓扑磁盘节点无型号**~~ ✅ **已修复（09-04）**：根因是 `core/system_info.py:_probe_disk_model_windows()` 用已弃用的 wmic（Win11 已移除该命令，`CommandNotFoundException`），永远返回 None，拓扑页兜底显示"磁盘1/磁盘2"。修复：改用 PowerShell `Get-CimInstance Win32_DiskDrive` 一次性获取所有物理磁盘型号，按分区顺序分配。验证：3 个分区均显示"ZHITAI TiPlus7100 2TB"，拓扑页磁盘节点显示真实型号。
4. ~~**拓扑页 SVG 布局混乱**~~ ✅ **已重写（09-04，Ponytail）**：原 SVG 手动算坐标（JS 436行 + CSS 512行 + main.css 205行），连线交叉不响应式。重写为纯 HTML 分层卡片：section 左侧色条 + div 节点卡片 + 原生 details 折叠 + ↓ 层间箭头。JS 436→280行，CSS 512→250行，删 main.css 205行死代码。
4. **网络上下行速率首帧为 null**：psutil 需两次采样算差值，第二次刷新才有值，前端已把 None 显示为「—」。
5. **改后端必须重启 8787 服务并强刷前端**：本次出现过「新前端 + 旧后端」导致页面错乱；重启要确认真正监听 8787 的 PID 启动时间在改码之后。
6. **`/api/free` 会超时**（环境问题非代码），前端已给 90s 超时；`/api/qos/check` 不存在（404），用 `/api/qos/status`。

---

## 四、最近变更（只留最近 10 条，更早见开发日志）
- 09-04 豆包：修复任务队列预算预检 R7 误拦截 bug。根因：route_helpers.py:build_gate_context() 取 REGISTRY.get("models",{})，但 registry.json 无顶层 models 键（模型在 ollama.models/comfyui.models），ctx.registry_models 恒为空导致所有已登记模型被 R7 误判"未登记"，前端预算预检永远报错、提交按钮禁用。修复为从 ollama.models+comfyui.models 构建。3 用例验证通过（SDXL/qwen 通过，未登记模型仍拦截）。用 diagnosing-bugs Skill 纪律化诊断（反馈循环→复现→假设→修复→回归测试）。
- 09-04 豆包：安装 Archify 架构图生成器（上游 tt-a1i/archify v2.17，MIT）到 .user_skills/archify/。VPS 中转下载（本机 GitHub 直连被 reset），doctor 全通过（Node v20 兼容）。生成 GMae 调度中心架构图：8 节点分层架构（Web UI→API网关→调度引擎/事件总线/核心设施→服务适配→Docker运行时→GPU），7 条连接，showcase 质量 0 错误，产出 ram-console/docs/gmae-console-architecture.json + .html（714KB 交互 HTML，支持 3 个视图聚焦/缩放/搜索/主题切换）。已更新根 AGENTS.md Skill 清单（7→8个）。

- 09-04 豆包：Ponytail 全仓库臃肿审计+清理。删除死代码约690KB/4000+行：legacy/web_v2_archive(329KB)+legacy/v1-index.html(132KB纯死文件,LEGACY_HTML被导入但从未读取)+ceng/(72KB认知引擎实验模块,零主代码引用)+cli/(55KB gmae-cli未集成)+meng/(17KB与model-benchmark-suite重复)+engine/event_bus.py(211行零引用,core/event_bus才是活跃版)+clients/helper_client.py(193行只自引用,services/helper才是活跃版)+tests场景测试4个+ceng/meng配套测试3个。清理config.py/route_helpers.py/server.py的LEGACY_HTML悬空引用。验证:server.py完整import通过,api.endpoints批量导入58路由注册正常。
- 09-04 豆包：安装并适配 Ponytail Skill（开源 82K+ Star 的"懒惰资深工程师"编码模式）到 `.user_skills/ponytail/SKILL.md`。中文翻译 + 整合 6 个子技能（主模式/review/audit/debt/gain/help）为单 Skill + 三档强度(lite/full/ultra) + GMae 定制规则（显存铁律优先/Docker禁令/看门狗唯一/蓝图禁擅改/收工五步）。已更新根 AGENTS.md Skill 清单（6→7个）。源码暂存 `_ponytail_src/`，确认无误后可删。
- 09-04 豆包：修复显存明细页进程“停止”操作失败（三层叠加 bug）。①前后端字段契约不一致：前端传 service、后端读 name，已在 api.js serviceAction 单点适配为 name；②后端 service_action/docker_action 白名单只有 comfyui/fooocus，漏了最常占显存的 ollama，已补 ollama；③dashboard.js 重复定义简陋版 _serviceAction 覆盖了 pages.js 公共完善版，已删除重复、收敛为一处；④vram 进程行新增 _stopGpuProcess 按 app 分发：ollama 走 /api/model 精准卸载单模型（不停容器）、comfyui/fooocus 走容器 stop。后端经 watchdog 自动重启，health 正常，非破坏 API + 浏览器分支逻辑全部验证通过。
- 09-04 豆包：修复 pages.js 拆分遗漏导致的全站页面崩溃。①vram.js 漏对外入口 vram()（Pages.vram not a function），从拆分前备份补回；②queue.js 漏 _loadQueue/_cancelTask/_clearDoneTasks/轮询方法，按后端真实字段（status=queued/precheck/freeing/running/done/failed/canceled、progress 字符串、created 时间戳）补齐，轮询带切页自愈+防重入；③给 9 个异步加载方法+settings 6 个子渲染方法统一切页竞态守卫（await 后用 Router.current / document.contains(el) 判活），根治快速切页 Cannot set properties of null。验证：21 JS 语法全过、3 轮高频快切 0 错误、11 页正常。

- 09-03 豆包：拓扑 5 层重构（system_info.py/topology.py/topology.js），装 psutil，修网络 IP 优先级（局域网优先）、WSL gbk/utf-16 编码、容器分类优先级（nextcloud-nginx 归 storage）、节点 None 显示。
- 09-03 豆包：MIT LICENSE（根+子项目）、THIRD_PARTY.md、开源合规指南、跨会话一致性方法论；开发日志归档 8 月（1636→139 行）。
- 09-03 豆包：前端 Day1-7 整改 + pages.js 拆 11 文件 + 导航显存条修复 + 桌面「调度中心启动.bat」路径修正（watchdog.py→engine\watchdog.py）。
- 09-03 豆包：后端 113 文件审计 77.5 分(B)、前端审计 67 分(B-)、API 实测（22 GET 全可用，19/20 POST 可用），产出 4 份 docs 报告 + 7 天路线图。
- 09-01 DSH：修看门狗链路（bat 路径、engine/watchdog.py 的 BASE_DIR、logger toast 参数冲突）。
- 09-01 豆包：gmae-cli v1.0（10 命令组/30+ API/零依赖）；中间层重构（45 端点迁 12 endpoints 模块）。
- 09-02 豆包：S1 轻量采集（缓存层/Docker events/并行超时/nvidia-smi 缓存）、S2 事件关联引擎（EventBus+9 根因规则）、S3 告警降噪、S4 前端重做全部完成。

> 更早：见 `16gb-ai-studio/docs/开发日志.md`（9 月）与 `devlog_archive/开发日志_202608.md`（8 月）。

---

## 五、稳定背景（少改）

### 项目身份
- 总项目 GPU Maestro 显存指挥家（GMae），核心引擎 Prism Engine(P-Eng)；首个子项目 16G-AI-Studio(16GAS)。
- 参赛：2026 上海开源软件应用创新大赛·智算云赛道，个人参赛，截止 10-11。许可证 MIT。
- 工作边界：只在 `D:\Users\Danny\Documents\GMae_Amanda` 内输出，与「家庭智能中枢」主仓库完全切割。

### 系统现状
- 调度中心：端口 8787，监听 0.0.0.0（局域网 http://192.168.1.8:8787/）；认证用 X-API-Key（.api_token）+ Session 登录。
- 硬件：RTX 4060 Ti 16GB / i5-13400TEF / 31.8GB 内存 / Win11(26200) / Docker 29.7.2 / WSL2。
- Docker：ollama、comfyui、open-webui、searxng、caddy、n8n、immich×4、nextcloud×5 等 15+ 容器。
- 看门狗：唯一 `engine/watchdog.py`（monitoring only），登记在 WATCHDOGS.md；用户手动双击 bat 启动（Agent 后台进程会被沙箱清理）。

### 绝对禁令（详见根 AGENTS.md，违反会损坏系统）
1. 禁止 `docker compose up -d comfyui`（清空容器内改动，改容器用 exec/cp + commit）。
2. 禁止显存打满（生成前释放到 <4GB）。
3. 禁止重复创建看门狗（先读 WATCHDOGS.md）。
4. 禁止用 junction/mklink 迁移 Docker 数据（用官方 Disk image location）。
5. 禁止盲目跑 >10GB 模型（先算峰值显存）。
6. 禁止凭文件大小判断下载完成（aria2 预分配，必须校验完整性）。
7. 🚫 蓝图 `docs/调度中心架构与交互设计.md` 禁止擅改，改动须先报主公批准。

### 关键技术坑
- `/api/health` 顶层必须含 services 字段，否则 watchdog 误判 half-dead 无限重启。
- WSL2 容器内 nvidia-smi 拿不到进程显存，进程账本用 ps_fallback 兜底；进程匹配用「llama-server」精确匹配（"ollama"含"llama"）。
- 改 server.py/后端后必须杀真正占用 8787 的 PID 再重启；重启会清空内存 session，浏览器需重登。
- Wan2.2-TI2V-5B 必须用 wan2.2_vae.safetensors（48通道），不能用 wan_2.1_vae（16通道）；16GB 卡只适合氛围类短片，复杂人物动作会崩。

### 关键文件指针
| 用途 | 位置 |
|------|------|
| 总入口/记忆架构/禁令 | `AGENTS.md`（根）、`16gb-ai-studio/AGENTS.md` |
| GMae 最高权威 | `docs/家庭智能中枢建设方案_总纲_v3.0.md` |
| 双最高指南 | `docs/显存管理最高指南.md`、`docs/代码工程最高指南.md` |
| 蓝图（禁擅改） | `16gb-ai-studio/docs/调度中心架构与交互设计.md` |
| 进度/日志 | `16gb-ai-studio/docs/项目进度跟踪.md`、`docs/开发日志.md` |
| 后端代码 | `16gb-ai-studio/vram-console/`（core/engine/services/api/endpoints） |
| 前端代码 | `16gb-ai-studio/vram-console/web/`（js/core、js/pages、js/components、css） |
| 看门狗登记 | `16gb-ai-studio/vram-console/WATCHDOGS.md` |
| 工程一致性规范 | `docs/跨会话多Agent工程一致性方法论.md` |
| 开源合规 | `16gb-ai-studio/THIRD_PARTY.md`、`docs/开源借鉴与许可证合规指南.md` |

---

## 六、新 Agent 上手顺序

1. 读本文件（任务锁 → 当前焦点 → Open Issues）。
2. 读根 `AGENTS.md` + `16gb-ai-studio/AGENTS.md`。
3. 按任务读对应最高指南（显存/代码工程）与蓝图相关章节。
4. `curl http://127.0.0.1:8787/api/health` 确认服务；动手前 grep/查代码确认无重复实现。
5. 认领任务锁 → 开工 → 收工按「收工五步」+ 更新本文件四段并释放锁。

---

*每次收工更新「一/二/三/四」与任务锁；最近变更超过 10 条时最旧的移入开发日志。*
